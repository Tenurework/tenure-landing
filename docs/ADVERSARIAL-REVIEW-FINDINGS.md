# Adversarial review — eight personas

Run 2026-08-03 against `refine/landing-bible-v1`. 144 findings: 22 P0, 53 P1, 47 P2, 22 P3.

Each persona read the source independently and reported its own first trust failure,
first comprehension failure, first blocked action, and any claim that would not survive a
live demo. Findings repeated by several personas are the strongest signal in this document.

Raw output follows, unedited.


========================================================================================
## Director, university Office of Student Engagement — steward of ~26 student organizations, the actual buyer, and the person who answers to the dean if this goes wrong
========================================================================================
FIRST TRUST FAILURE: /story:189-192 — \"Every organization Simon's Office of Student Engagement stewards puts Tenure to work, and so does the office itself.\" Present tense, under a heading that says \"Early, and honest about it.\" I have signed nothing. After reading that I went back and started checking whether the careful hedging elsewhere on the site was real or decorative.
FIRST COMPREHENSION FAILURE: Who actually does the work in week one. /pilot:146 says \"We bring in each org's current drive and folders\" and :131 says \"You work directly with [both founders]\" — but I cannot tell whether my staff export the drives, whether the student officers do the data entry, or whether two founders genuinely intend to hand-onboard twenty-six organizations in a single term. That is the number that decides whether this gets adopted or abandoned, and the page does not contain it.
FIRST BLOCKED ACTION: I wanted to find out what the pilot costs my office — in dollars and in staff hours — and could not. /pilot never mentions either; the only pricing sentence on the entire site is Faq.tsx:45, \"Pricing is per portfolio, not per club\", which names a shape and no number, and does not say whether the pilot term itself is free.
WOULD NOT SURVIVE A DEMO: The AI answers. If I ask \"How do we run elections?\" and expect the \"Bylaws §4\" citation the site shows twice (Platform.tsx:126, AiOnboarding.tsx:28), I get nothing — document file contents are not indexed and policies are not in the corpus. Same for \"what did we overpay last year?\" answered with \"$1,240 over\" (SeatMechanism.tsx:272) and the hero dashboard's own suggested prompts \"Why was the gala budget returned?\" and \"Which roster changes did OSE approve?\" (DashboardMock.tsx:23-29): finance figures and people records are not in the corpus at all. Every one of those is a question the site invited me to ask.

--- [P0] /story states my office and all 26 of my orgs are already running Tenure
  WHERE: src/app/story/page.tsx:189-192
  src/app/story/page.tsx:189-192 reads: "Here is exactly where things stand, and where they go this fall. Every organization Simon's Office of Student Engagement stewards puts Tenure to work, and so does the office itself." Present tense, stated as accomplished fact, under a section headed "Early, and honest about it." Ground truth: verbally agreed, not contracted, and today is 2 August 2026 — the fall term has not started. I have signed nothing. If my dean or my provost's office reads that sentence, the question I get is not about software, it is about whether I committed the office to a vendor without procurement. Note also that site.ts:48-58 governs exactly this and forbiddenPilotPhrases (claims.ts:688-693) tests for it — but the regexes look for "is rolling out|deploying|standing up", so "stewards puts Tenure to work" walks straight past the guard. The control exists and this sentence defeated it.
  FIX: Rewrite to the tense the rest of the site uses: "This fall we are planning to run Tenure with the organizations Simon's Office of Student Engagement stewards, and with the office itself. Scope and timing are proposed, not contracted." Then broaden the forbiddenPilotPhrases regex to catch any present-tense verb attaching the office to the product — e.g. /Office of Student Engagement[^.]{0,80}\b(puts|runs|uses|is on)\b/i.

--- [P0] /pilot publicly enrols my student organizations before I have agreed
  WHERE: src/app/pilot/page.tsx:212-215
  src/app/pilot/page.tsx:212-215: "One term, hands-on the whole way. No waiting list, no application, and no placeholder accounts. If OSE stewards your organization, you're in." That sentence is addressed to my club presidents, on a public indexable page (routes.ts:48-55), and it tells them their participation is already settled. It is not. The practical consequence is concrete: twenty-six boards read "you're in", email my office asking when they get accounts, and I am the one explaining that no agreement exists. It also converts the office's decision into a fait accompli — the page has recruited my orgs and left me to ratify it.
  FIX: Make the office the gate, not the bypass: "The proposed scope is every organization the office stewards. If your office decides to run the pilot, no organization has to apply separately." Remove "you're in" entirely until a written agreement exists.

--- [P0] /pilot tells me advisors see only their own orgs; /trust says they see everything
  WHERE: src/app/pilot/page.tsx:126
  src/app/pilot/page.tsx:126 (the "A console for the office itself" card): "Three staff levels, so an advisor sees advising and a director sees everything." That is a read-scope claim and it is false. src/app/trust/page.tsx:64-69 says the opposite: "Any account with an institution membership can currently read every organization in the portfolio. An advisor relation exists in the schema but does not narrow reads." claims.ts C-025 marks it 'unsupported' with the note "Do not claim 'least access by default'." This is not academic for me. My advisors are faculty and grad assistants attached to one organization each. If I provision nineteen advisor accounts on the strength of that sentence, I have handed every one of them read access to twenty-six organizations' budgets, member rosters, reimbursement requests and uploaded documents. I find that out at the security review, after I have already told my staff otherwise. The true statement lives on a page I was never required to visit; the false one lives on the page you sent me to.
  FIX: Delete the read-scope half of the sentence: "Three staff levels — a director inherits everything staff can do, and staff everything an advisor can do — and an override lands on the record like every other decision. Today any institution account can read every organization; per-advisor scoping is not built (see /trust)." The honesty already exists on /trust; it needs to be on the page the buyer reads.

--- [P1] Every AI demo on the site shows a question the shipped assistant cannot answer
  WHERE: src/components/home/Platform.tsx:118-127, src/components/home/AiOnboarding.tsx:26-29, src/components/home/SeatMechanism.tsx:269-274, src/components/visuals/DashboardMock.tsx:23-29
  Retrieval is keyword AND-matching over five record kinds; document file contents are not indexed, and finance figures and people records are not in the corpus at all (trust/page.tsx:156-162 says so). Yet: (a) Platform.tsx:118-127 and AiOnboarding.tsx:26-29 both show "How do we run elections?" answered with "Nominations open week 10, two-week window, ranked-choice ballot" cited as "Bylaws §4 · 2 records" — a section-level citation into a document whose contents are not indexed, and claims.ts C-020 explicitly says "Do not imply Tenure AI can answer policy questions"; (b) SeatMechanism.tsx:269-274 asks "Who's our caterer, and what did we overpay last year?" and answers "The '25 gala ran $1,240 over" — a finance figure; (c) DashboardMock.tsx:23-29 seeds the hero product surface with "Why was the gala budget returned?" and "Which roster changes did OSE approve?" — finance and people. I will type one of these on the walkthrough call, because they are the ones you showed me. Four sections of the site are advertising the two capabilities the corpus specifically excludes.
  FIX: Re-cut every demo prompt to the five indexed kinds: knowledge cards, document titles/descriptions, approvals, events, organization records. "Why did we drop the fall mixer?" (Memory) and "What's our sponsorship pipeline?" are already correct — use that register everywhere. Drop the "Bylaws §4" citation and any dollar figure presented as an AI answer, or add the one-line limit you already wrote on /trust.

--- [P1] /pilot answers none of the four questions a buyer brings to it — and its own meta description promises all four
  WHERE: src/app/pilot/page.tsx (whole page) vs src/lib/routes.ts:51
  routes.ts:48-55 sells the page as: "A planned Fall 2026 pilot with Simon's Office of Student Engagement: scope, who does what, onboarding inputs, support model and how success is measured." That is exactly my checklist. The page delivers none of it. There is no cost (nothing on the site names a number; the only pricing sentence anywhere is Faq.tsx:45, "Pricing is per portfolio, not per club", which is a shape not a price, and does not say whether the pilot term itself is free). There is no staff-hours estimate. "Who does what" is one card, "a direct line to the founders" (pilot/page.tsx:131). "Onboarding inputs" is "we bring in each org's current drive and folders" with no statement of who exports the drives, who cleans them, or whether my staff or the students do the data entry. There is no success measure at all — nothing tells me what I would report to my dean in December to justify continuing. And nothing says what happens at term's end: whether the record stays, what it costs afterwards, or how we get our data out (bulk export is roadmap per trust/page.tsx:126-129, so the answer is "we email you and a founder does it by hand" — which I would rather learn now).
  FIX: Add a fifth section, "What it costs you," with four honest rows: money for the pilot term; estimated hours from my staff in weeks 1-2 and per month after; exactly which artifacts we must supply (roster CSV, drive export, budget spreadsheet, handbook PDFs); and what happens at term's end including the manual export path. Then add a "How we'll know it worked" block with two or three measures you can actually count from the record (seats filled with named shadows before rotation; approvals cleared inside the 3-day flag; handoff packets opened by incoming officers). If any row cannot be filled honestly today, say so — that is still an answer.

--- [P1] "No new tool to learn" is contradicted by the same page, and it is the exact sentence a burned buyer stops reading at
  WHERE: src/app/pilot/page.tsx:295-296
  src/app/pilot/page.tsx:295-296: "No big migration and no new tool to learn. The pilot runs in four moves across a single term." Four lines above it, step 01 is "We bring in each org's current drive and folders" — that is a migration. Step 02 is "Run the term in Tenure... one system instead of a dozen scattered tools" — that is a new tool, and the entire value proposition depends on students logging finances, events, members and decisions inside it all term. I have bought software that promised this. It is why I am sceptical. Claiming zero adoption cost for a system-of-record replacement reads as either naive or dishonest, and it undercuts the credibility the rest of the site works hard to build.
  FIX: Replace with the real shape of the ask: "There is a new system to learn — it is where the work happens, or the record does not fill. What there isn't is a migration project: we do the first import from your existing drives with you, and after that the record fills as officers do the work they were already doing."

--- [P1] There is no legal entity to contract with, and the page written for institutional review does not say so
  WHERE: src/app/trust/page.tsx:225-232 and src/app/pilot/page.tsx (absent)
  claims.ts C-032 records that no entity has been formed. That fact appears in exactly two places on the site: the warning banners on /privacy:24-25 and /terms:26-27. /trust — the page /contact:63-76 explicitly points institutional evaluators at, and the page my procurement office will actually read — says only "an early-stage product run by two founders" (trust/page.tsx:231) and, at the very bottom, that the legal docs are founder drafts (trust/page.tsx:332-335). /pilot, which asks me to commit my entire portfolio, says nothing. For me this is not a footnote: it is the single most decision-relevant fact on the site, because it means there is currently no counterparty for a purchase order, no vendor to insure, no one to sign a data-processing agreement, and nobody with a balance sheet if the record is lost. I should not discover it three weeks into procurement.
  FIX: Add a "Company" group to /trust with a 'Not supported' badge: no incorporated entity today, no insurance, no counsel-reviewed agreement, and what the founders intend to do about it before a pilot agreement is signed. Repeat one line of it on /pilot next to "Scope and timing are proposed, not contracted."

--- [P2] "Immutable audit trail" on the home page contradicts your own trust page
  WHERE: src/components/home/TrustStrip.tsx:13
  src/components/home/TrustStrip.tsx:13 puts the chip "Immutable audit trail" on the home page, second section, beside the university logos. trust/page.tsx:96-101 says the opposite in detail: no hash chain, no signature, no checksum, "Append-only is enforced by the application, not by cryptography or by write-once storage" — and then, admirably, "Any vendor claiming immutability should be asked which of those three it means." You are the vendor claiming immutability, on the home page. claims.ts C-031 forbids "cryptographically immutable"; the forbidden-phrase regexes catch "tamper-proof" and "hash-chained" but not the bare word "immutable", so this shipped. The chip also carries none of the C-004 qualification that coverage is 49 of 63 server actions — my auditor will ask, and "immutable" will be the word they quote back.
  FIX: Change the chip to "Append-only audit trail" — which is what Platform.tsx:262 already calls it, and what it actually is. Add /\bimmutable\b/i to forbiddenPhrases under C-031.

--- [P2] The university lockup is captioned as a pilot participant, and Startup Wednesday is swept in with it
  WHERE: src/components/home/TrustStrip.tsx:20-37, src/components/site/SupporterStrip.tsx:11
  src/components/home/TrustStrip.tsx:20-37 renders the label "Planned Fall 2026 pilot" immediately beside the Simon Business School / University of Rochester horizontal lockup AND the Startup Wednesday mark, as a single row. The caption reads as "these are the pilot participants." Startup Wednesday has nothing to do with the pilot. claims.ts C-022 permits the marks only to "indicate origin and support" and says they "must never be captioned in a way that implies customership, sponsorship of the product, or endorsement" — a pilot caption is precisely that. The unused SupporterStrip.tsx:11 goes further with "Supported by" over the U-of-R lockup, which asserts the university supports a private company. My institution's trademark office polices this, and the complaint would land on my desk because the pilot is mine.
  FIX: Separate the two claims. Caption the marks "Founded at" / "Built with" (the wording /story:26 already uses) and move the "Planned Fall 2026 pilot" label away from the logo row, or drop the Startup Wednesday mark from any row a pilot label touches. Confirm the exact caption wording against the written permission you hold before shipping.

--- [P2] The metrics band reads as adoption evidence and is headed by a promise the numbers do not keep
  WHERE: src/components/home/MetricsBand.tsx:68-75, src/lib/site.ts:120-149
  MetricsBand.tsx:68-75 heads four tiles with "Continuity, measured", "We don't measure engagement. We measure whether the knowledge survived", and "Every number here is counted, not projected." The tiles (site.ts:120-149) are: 26 organizations modelled, 2-gate approval chain, 0 records deleted at offboarding, 132 end-to-end tests. None of them measures whether knowledge survived, because the pilot has not run — claims.ts C-014 says so explicitly: "structural counts from the seeded model, not a count of active users. Never present them as customers, users or adoption." A giant "26" under "measured" is read as adoption by every scanning eye, including my dean's. "0 records deleted" is a design property dressed as telemetry — nothing has ever been offboarded in production, so the zero is not counted from anything. And the sub-line on the first tile — "built from the office's own leadership roster rather than invented for a demo" — raises a question nobody answers: my students' names and seat assignments are in your seeded dataset, and I do not recall being asked.
  FIX: Retitle the section to what it is — "What we can show you today" — and drop "We measure whether the knowledge survived" until something has been measured. Label the 26/209 tile "modelled from the office's roster, seeded — not users" on its face, not in a register. And add one line stating whether the seeded roster uses real names or is anonymised; if real, say who approved it.

--- [P2] In dark mode the university and Startup Wednesday logos disappear
  WHERE: src/components/home/TrustStrip.tsx:33, src/components/site/SupporterStrip.tsx:20
  TrustStrip.tsx:33 and SupporterStrip.tsx:20 apply `mix-blend-multiply` to the logo images. In dark mode `bg-cloud` resolves to `--surface` = oklch(21.5% 0.021 250) (globals.css:283 and :112). Multiply blends the navy artwork against a near-black backdrop, producing something darker than the backdrop — the lockups render as invisible or as a black smear, leaving the label "Planned Fall 2026 pilot" pointing at two empty gaps. The blend mode is there to knock out the PNGs' white boxes, which only works on a light background. Roughly half of visitors default to dark.
  FIX: Ship pre-cut transparent-background assets and drop the blend entirely, or gate it: `mix-blend-multiply dark:mix-blend-normal` plus a light plate behind the marks in dark mode (`dark:bg-white/90 dark:rounded-md dark:px-3 dark:py-1.5`), which is how the university's own brand guidance usually wants the navy lockup placed on dark anyway.

--- [P2] "Not a pitch deck. The system, actually running" sits over hand-drawn mockups
  WHERE: src/components/home/ProductAtWork.tsx:200
  ProductAtWork.tsx:200 headlines the section "Not a pitch deck. The system, actually running." What follows is FinanceCard and HandoffCard (ProductAtWork.tsx:13-138) — hand-built divs with animated progress bars and a card that slides from an "Outgoing" box to an "Incoming" box. Same for DashboardMock on the hero. They are good mockups, but they are drawings, and the code comments admit it. A buyer who has sat through vendor demos recognises a synthetic UI instantly, and the claim "actually running" attached to one is the moment I start discounting everything else on the page. You have a deployed application with 132 e2e tests; the honest version of this section is stronger than the claim.
  FIX: Either retitle to something the artwork supports — "How it behaves" — or replace the two cards with real screenshots from the deployed app with seed data. The latter would also let you delete a lot of animation code.

--- [P3] The same headline runs twice, in adjacent home-page sections
  WHERE: src/components/home/Handoff.tsx:287-288 and src/components/home/AiOnboarding.tsx:77-78
  Handoff.tsx:287-288 renders the h3 "They read the seat before they sit in it." AiOnboarding.tsx:77-78 renders the h2 "They read the seat before they sit in it." They are consecutive sections on the home page — I scroll past the sentence, then hit it again as the next section's headline and briefly think the page has looped. The page-level comment in app/page.tsx:22-23 says the refinement pass existed because "Four arguments were made twice"; this one survived.
  FIX: Keep it on AiOnboarding, where the section is entirely about pre-term reading. Retitle the Handoff sub-block to what its three stages actually show: "Shadow, active, alumni — one status change, no file transfer."

--- [P3] "Contact Sales" survives in the footer and FAQ after the site banned it
  WHERE: src/components/site/SiteFooter.tsx:45, src/components/home/Faq.tsx:94
  site.ts:23-27 sets one conversion phrase, "Book a walkthrough", with the reason written down: "'Contact Sales' oversold a two-founder company and set the wrong expectation for who picks up." SiteFooter.tsx:45 hardcodes "Contact Sales" and Faq.tsx:94 hardcodes "Contact sales", both bypassing site.ctaLabel. Minor, but it is the wrong promise on the two links most likely to be clicked by someone who scrolled to the bottom — and /contact:11-12 then tells them "There is no SDR." Related: the footer asserts "© 2026 Tenure. All rights reserved." for a business that /privacy:24-25 says is not an incorporated entity.
  FIX: Let both render `site.ctaLabel` (ContactSalesLink already defaults to it — just remove the children). Consider "© 2026 Almamy Diaby and Satvik Adyanthaya" in the footer until an entity exists.

--- [P3] The claims register points at a component that is no longer on the site
  WHERE: src/lib/claims.ts:599, src/components/site/SupporterStrip.tsx
  claims.ts:599 lists C-022's locations as ["components/site/SupporterStrip.tsx", "/", "/story"]. SupporterStrip is defined but imported nowhere — the home page uses TrustStrip instead, and /story renders the marks as a text row. So the register's 'where' for the trademark claim points at dead code and misses TrustStrip.tsx, the one place the marks actually appear. The register is the control that makes the rest of this site trustworthy; when it drifts, the guard stops guarding the real page.
  FIX: Delete SupporterStrip.tsx or wire it back in, and point C-022's `where` at components/home/TrustStrip.tsx. Worth adding a test that fails when a `where` entry names a component with zero importers.

========================================================================================
## Hard-nosed operations leader; forty tools evaluated; assumes every vendor is exaggerating and hunts for the seam between the demo and the mechanism.
========================================================================================
FIRST TRUST FAILURE: The trust strip: the label "Planned Fall 2026 pilot" sitting in one row with the University of Rochester lockup. I went to /story to check the relationship and found "Every organization Simon's Office of Student Engagement stewards puts Tenure to work" — present tense — under a heading that says "Early, and honest about it." Nothing is contracted. That is where I started discounting everything else.
FIRST COMPREHENSION FAILURE: What Tenure AI can and cannot answer. The home page demos a budget-variance question with a dollar figure and cites a bylaws section; the trust page says finance figures, people records and document contents are all out of the corpus. After reading both I could not tell you which questions a new officer can actually ask, and the two pages cannot both be right.
FIRST BLOCKED ACTION: Finding out what it costs. The only pricing statement on the site is a pricing shape with no number, buried as the fourth item of a collapsed FAQ, and it does not say whether the Fall 2026 pilot is free or paid — so I cannot open a budget conversation internally after reading the entire site.
WOULD NOT SURVIVE A DEMO: "Who's our caterer, and what did we overpay last year?" → "$1,240 over, renegotiated to a flat $3,800." It is rendered on the home page as the proof of the differentiator, and the retrieval corpus contains neither finance figures nor people. Ask "are we on budget?" on a shared screen and the product returns keyword-matched knowledge cards with no number in them. Second on the list: "Bylaws §4" as a cited source, when document contents are not indexed and policies are not in the corpus at all.

--- [P0] The AI demo that sells the product is the exact demo the product fails: a budget-variance question answered with a dollar figure
  WHERE: src/components/home/SeatMechanism.tsx:269-275 (contradicts src/app/trust/page.tsx:159-161)
  SeatMechanism is labelled "Why Tenure is different" — the differentiator section. Its live mock asks "Who's our caterer, and what did we overpay last year?" and answers "Prestige Catering. The '25 gala ran $1,240 over, Marcus renegotiated it to a flat $3,800. 3 sources." That answer requires (a) budget-vs-actual finance figures and (b) a person attribution. Your own /trust page, line 161, says: "Finance figures and people records are not in the corpus, so those questions cannot be answered by the assistant today." So the home page's headline proof and the trust page's confession are the same sentence with opposite signs. I will ask for exactly this on the call — "are we on budget?" is the first question any ops person asks a memory tool — and the assistant will return either nothing or ranked knowledge cards with no number in them. That is the moment the meeting ends.
  FIX: Replace the exchange with something inside the five indexed record kinds and outside finance: e.g. "Who did we use for catering last year, and why did we switch?" -> "Prestige Catering, from the '25 gala vendor card; the prior vendor was dropped after the run-of-show lesson filed by Marcus Lee." No dollar figures, no computed variance. Then add one visible line under the mock: "Tenure AI reads knowledge cards, document titles, approvals, events and org records. It does not read the ledger — for money, open Finance." Say the limit where you make the promise, not four clicks away.

--- [P0] /story states as present fact that every OSE-stewarded organization is running Tenure
  WHERE: src/app/story/page.tsx:187-192
  "Every organization Simon's Office of Student Engagement stewards puts Tenure to work, and so does the office itself." Present tense, declarative, unqualified — and it sits directly under the heading "Early, and honest about it." Ground truth: the Fall 2026 pilot is verbally agreed and not contracted, and no organization is using the product. This is the single sentence I would read back to you in the first meeting, because everything else on the site becomes negotiable once I catch one sentence claiming deployment that has not happened. Your own governance file (src/lib/site.ts:48-58) forbids exactly this framing, and the guard regex in claims.ts:689 only catches "is rolling out / deploying / standing up" — it misses "puts Tenure to work" entirely.
  FIX: "This fall, we are planning to put Tenure in front of every organization Simon's Office of Student Engagement stewards, and the office itself. Scope and timing are proposed, not contracted." And add a verb-agnostic guard: flag any sentence containing "Office of Student Engagement" whose main verb is present-tense indicative, rather than enumerating three phrasings.

--- [P0] /pilot enrols organizations on behalf of an office that has signed nothing
  WHERE: src/app/pilot/page.tsx:211-214
  "No waiting list, no application, and no placeholder accounts. If OSE stewards your organization, you're in." That is a public, written commitment made on a third party's behalf, on a page whose own header says "Scope and timing are proposed, not contracted" — 35 lines earlier. If a club president reads that and shows up expecting an account, the office finds out that a vendor promised access to its portfolio before signing anything. That is how you lose an office, and it is the sentence their general counsel will screenshot.
  FIX: "The proposed scope is every organization OSE stewards — no application and no waiting list within that scope. Enrolment follows a written agreement with the office, which does not exist yet." Keep the header's qualifier in the body of every section that describes who gets in.

--- [P0] Two AI mocks cite "Bylaws §4" as a source — the assistant cannot read document contents or policies
  WHERE: src/components/home/AiOnboarding.tsx:25-29 and src/components/home/Platform.tsx:119-127
  AiOnboarding shows "How do we run elections?" -> answer, cited to "Bylaws §4 · 2 records". Platform's Tenure AI hero cell shows the identical citation. Document file contents are not indexed — only titles and descriptions — and your own claims register, C-020, says in terms: "policyText() has zero callers and policies are NOT in the AI corpus. Do not imply Tenure AI can answer policy questions." A §-level citation into a governing document is the strongest possible implication of full-text grounding, and it appears twice on the home page. A reviewer who takes the citation at face value will upload a handbook, ask a question about clause 3, and get nothing.
  FIX: Drop the §-level document citation everywhere. Cite only what the corpus contains: "Elections playbook card · 2 records". If you want document grounding in the pitch, ship indexing first — a keyword index over extracted text is a week of work and it would make this the honest claim it currently pretends to be.

--- [P1] The hero product surface says "Ask anything" — the forbidden phrase — and pins it to the module with no corpus
  WHERE: src/components/visuals/DashboardMock.tsx:452
  DashboardMock's AI panel renders "Ask anything about the {module} this seat has handled." The mock auto-tours and opens on Finance, so the first thing rendered above the fold is "Ask anything about the finance this seat has handled" — against a corpus that contains no finance. C-007 says "Never say 'instant', 'never invents' or 'answers anything'", and SeatMechanism.tsx:265 carries an explicit code comment saying you fixed this — but the guard regex is /answers? anything/, which does not match "Ask anything", so the hero kept it. The register is enforcing the string it was written with, not the claim.
  FIX: "Ask what this seat recorded about {module}." And widen the regex to /\b(ask|answers?)\s+anything\b/i so the guard tracks the claim rather than one phrasing. Same pass should catch DashboardMock.tsx:459 and product/page.tsx:86, which both promise answers link back to "files" — file contents are not indexed.

--- [P1] The hero renders "policy v4 snapshot" on an approval card — the one thing the register explicitly says you cannot imply
  WHERE: src/components/home/HeroFloatingCards.tsx:59-61
  HeroFloatingCards shows "Approval cleared · Spring Gala budget, $4,200 · policy v4 snapshot". C-005's qualification is unambiguous: "The stored policySnapshot is a thin flag object and is never read back. Do NOT claim the product can 'prove the rules in force' or reconstruct a policy version." A versioned policy snapshot on an approval record is precisely the control an auditor asks about — "show me the rules that were in force when this was approved" — and it does not exist. This is worse than a vague overclaim; it is a specific, falsifiable artifact rendered on the landing page.
  FIX: Replace with something the record actually holds: "decided by SCC-PRES · append-only step". Add "policy v\\d" and "snapshot" to forbiddenPhrases under C-005.

--- [P1] /pilot claims scoped advisor visibility; /trust says any institution account reads every organization
  WHERE: src/app/pilot/page.tsx:124-127 (contradicts src/app/trust/page.tsx:63-69)
  Pilot: "Three staff levels, so an advisor sees advising and a director sees everything." Trust: "Any account with an institution membership can currently read every organization in the portfolio. An advisor relation exists in the schema but does not narrow reads." Sixteen capabilities across three tiers governs what an advisor can DO; it does not govern what an advisor can SEE, and the pilot page's phrasing reads as read-scoping to anyone doing a security review. This is the contradiction that gets found in the questionnaire stage, after the pitch, which is the most expensive place to find it.
  FIX: "Three staff levels, so an advisor can act on advising and a director on everything. Read access today is portfolio-wide for any institution account — see the trust page." Never let a capability-tier sentence stand next to the word "sees".

--- [P1] The metrics band promises measured outcomes and delivers four structural counts
  WHERE: src/components/home/MetricsBand.tsx:68-75 and src/lib/site.ts:120-149
  The heading is "Continuity, measured" / "We don't measure engagement. We measure whether the knowledge survived." None of the four tiles measures whether knowledge survived. 26 organizations is seed data from a roster. 2-gate / 7 types is a schema fact. 132 e2e tests is a test count. And "0 records deleted at offboarding" is not a measurement at all — it is a design invariant, and it reads as 0 for the trivial reason that nobody has ever offboarded in production. Dressing a design invariant as an observed count is the oldest move in the deck, and the sub-line "Every number here is counted, not projected" makes it worse by inviting the scrutiny. To your credit, none of the four is fabricated — the problem is the frame, not the arithmetic.
  FIX: Retitle to what it is: "Continuity, built in" / "Four properties of the system, counted from the repository that deploys." Change the third tile to "deletion refused" with sub "a seat carrying history cannot be deleted, only retired" — state the invariant as an invariant instead of as a zero. When the pilot produces a real number, that is when "measured" earns its place.

--- [P1] "26 organizations modelled, 209 seats" sits in a traction band and will be read as customers
  WHERE: src/components/home/MetricsBand.tsx:78-86, src/lib/site.ts:122-126
  The claims register is careful — C-014: "These are structural counts from the seeded model, not a count of active users. Never present them as customers, users or adoption." The tile sub-line does say "built from the office's own leadership roster rather than invented for a demo," which is a real attempt. But the number sits as tile one of four under a heading about measurement, rendered with an animated count-up, in the position every SaaS page uses for logos and adoption. "Modelled" is doing a lot of load-bearing work in a 1.02rem label under a 3.6rem number. Half the people who see this will repeat it internally as "they have 26 orgs."
  FIX: Kill the count-up animation on that tile and change the label to "organizations modelled — none live yet". A number you deliberately refuse to inflate is worth more than one I have to discount. Also: the site has no customers section, no logos, no case study, and no testimonial — which is correct and honest, but it means this band is the only thing shaped like traction on the page, and it will absorb that reading whether you want it to or not.

--- [P1] /contact promises to open "a real organization's record" on the walkthrough
  WHERE: src/app/contact/page.tsx:14-17
  "We open a real organization's record — seats, approvals, the budget, the handoff packet — and you ask what you want to see." There is no real organization's record; there is a seeded model built from a roster. So on the call, one of two things is true: the record is seed data and you have oversold the demo, or it is genuinely someone's record and you are showing a third party's finances and roster to a prospect. The second reading is the one an institutional buyer will land on, and it is a data-handling question, not a marketing one.
  FIX: "We open a fully modelled organization — 26 orgs and 209 seats built from a real leadership roster, with the names changed — and you ask what you want to see." Precision here costs you nothing and buys the whole trust page's credibility.

--- [P1] Privacy promises deletion on request; the product's headline property is that it refuses deletion
  WHERE: src/app/privacy/page.tsx:144-150 (vs src/app/trust/page.tsx:120-124 and src/lib/site.ts:136-141)
  Privacy, "Your choices": "You can request access to the information we hold about you, ask for an export, or request deletion... we'll work with you and your organization to honor the request." Meanwhile the metrics band advertises "0 records deleted at offboarding", the trust page says "History cannot be deleted — a seat carrying assignments, holdings or knowledge is refused deletion," and the audit trail is append-only with no delete path anywhere in the application. A student asking for erasure of their contributions cannot be honoured without breaking the invariant you sell. You have not decided which one wins, and the two documents each promise the opposite.
  FIX: State the actual position: "We can delete or redact your personal identifiers on request. We cannot delete the organizational record itself — decisions, approvals and audit rows belong to the seat and the organization, and removing them is refused by design. Tell us which you need and we will explain exactly what we can and cannot remove." That is defensible; the current pair is not.

--- [P1] "Productive in days, not a semester" survives on /product after being deleted from the home page as unmeasurable
  WHERE: src/app/product/page.tsx:213-216; also src/app/pilot/page.tsx:159-162
  Problem.tsx carries the comment: "'The next leader is productive in days' was an outcome nothing measures; shadow access before the term starts is the mechanism that was meant by it." The line was correctly removed from the home page — and it is still an H2 on /product, reinforced by pilot step 04's "productive from the very first meeting" and product POINTS "Ready on day one". Nothing in either repository measures onboarding duration. The refinement pass found the defect, fixed one instance, and left three.
  FIX: "Reading the seat before day one, not a semester of relearning." Then grep the whole site for duration-outcome claims (days / weeks / semester / day one / first meeting) and hold them to the same rule you applied on the home page.

--- [P1] Tenure AI is described as answering from "the seat's full record" and "anything in" the handoff packet
  WHERE: src/app/product/page.tsx:80-87; src/components/home/HowItWorks.tsx:56-59; src/components/visuals/DashboardMock.tsx:459
  Three sentences make the same overreach. product/page.tsx:82: "Tenure AI answers from the seat's full record from day one." HowItWorks.tsx:59: they open the handoff packet "then ask Tenure AI about anything in it." DashboardMock.tsx:459: "Answers link the records, files, and decisions behind them." The handoff packet is DB-assembled and contains exactly the two things the corpus excludes — where the budget stands, and who held each seat with their contact address. So "ask AI about anything in it" points a new officer at the packet and invites the two questions that will fail. "Full record" and "files" are the specific words a keyword index over five record kinds cannot support.
  FIX: Everywhere: "answers from what the seat recorded — knowledge cards, approvals, events, org records and document titles." Enumerating five things reads as more credible than "full record", not less. Drop "files" until file contents are indexed.

--- [P1] "Immutable audit trail" as a home-page chip, over a trail with no cryptographic control and 78% coverage
  WHERE: src/components/home/TrustStrip.tsx:13 (vs src/app/trust/page.tsx:84-101)
  TrustStrip chip: "Immutable audit trail". The trust page's own limit says coverage is 49 of 63 server actions, that messaging, feed, profile and resource writes append nothing, that AI and search queries are not recorded at all, and that there is no hash, signature or checksum — append-only is enforced by application code, not by the database or write-once storage. Your forbidden-phrase list bans "tamper-proof", "hash-chained" and "cryptographically immutable" but not the bare word "immutable", which is the strongest of the four to a buyer. And the trust page even coaches me to ask the question: "Any vendor claiming immutability should be asked which of those three it means." So the site sets the trap and then walks into it four sections earlier.
  FIX: "Append-only audit trail" — the same word the Platform card already uses correctly at Platform.tsx:262. Add /\bimmutable\b/ to forbiddenPhrases under C-031.

--- [P1] The University of Rochester and Startup Wednesday marks are captioned "Planned Fall 2026 pilot"
  WHERE: src/components/home/TrustStrip.tsx:20-38; src/lib/claims.ts:596-612; src/components/site/SupporterStrip.tsx (unreferenced)
  TrustStrip renders the label "Planned Fall 2026 pilot" immediately to the left of the Simon/U-of-R lockup and the Startup Wednesday mark, in one flex row. C-022 permits the marks for origin and support only, and states they "must never be captioned in a way that implies customership, sponsorship of the product, or endorsement." Placing both under a pilot label does exactly that — and Startup Wednesday is not a pilot party at all, so the grouping is wrong on its own terms. Separately: C-022's `where` field points at components/site/SupporterStrip.tsx, which is dead code — it is imported nowhere, so the register is governing a component that does not render and is silent about the one that does.
  FIX: Split the row: "Founded at" over the Simon lockup, "Supported by" over Startup Wednesday, and move "Planned Fall 2026 pilot" to the chip list where it belongs next to the other scope chips. Then fix C-022's `where` to name TrustStrip, and either delete SupporterStrip or render it.

--- [P2] Pricing is a sentence in a collapsed FAQ, has no number, and has no row in the claims register
  WHERE: src/components/home/Faq.tsx:43-46; src/lib/claims.ts:3-5
  The only pricing on the site: "Pricing is per portfolio, not per club: one budget line for every organization an office stewards." No number, no range, no "starts at", no statement of what the pilot costs (free? paid? unpriced?), and it is the fourth item in an accordion that opens on item three. That is a pricing model, not a price — I cannot take it to a budget conversation. It is also a material commercial claim with no entry in claims.ts, which asserts it holds a row for "every material claim the public site makes about the product, the pilot, the company or its security posture."
  FIX: Say the pilot term is unpriced or name the number. "The Fall 2026 pilot is unpriced — we are buying evidence, not revenue. After it, pricing is per portfolio: one budget line for every organization an office stewards, in the $X–$Y range for a portfolio this size." And add a C-0xx row for pricing so the register's stated scope is true.

--- [P2] The claims register does not in fact cover every material claim on the site
  WHERE: src/components/home/TrustStrip.tsx:11-13, src/components/home/Platform.tsx:192-200, 304-316, 274-286, 289-301; src/lib/claims.ts:1-21
  The file's opening comment says every material product claim has a row. Checked against the rendered copy, these appear on the home page with no register entry and no evidence citation: "Hard + soft conflict detection" (TrustStrip chip and a Platform card, with a specific rendered artifact — "Hard conflict, Schlegel 207, 5:00 to 6:30p" — repeated in the hero float and the dashboard mock); "Reports & search — spending, participation and continuity, one report / board-ready"; "Messages — read and post rules differ by conversation type"; "Cross-org collaboration — co-hosting between orgs, same approval path". Conflict detection in particular is one of four chips in the trust strip, which is the highest-density claim surface on the site. The register's value is that it is exhaustive; four uncovered home-page claims is enough that I stop treating it as exhaustive.
  FIX: Add rows with evidence for conflict detection, reports, messaging and co-hosting, or remove the claims. Then add a test that inverts the current direction: not only "does forbidden text appear" but "does every product noun rendered on the site map to a claim id" — the current suite can only catch phrasings someone already thought of.

--- [P2] Everything an institutional buyer needs after the demo is absent
  WHERE: src/app/trust/page.tsx (all groups); src/app/terms/page.tsx:96-125; src/app/privacy/page.tsx:129-150
  Working through my standard checklist against the whole site: no availability or uptime commitment (Terms says only "we don't guarantee uninterrupted availability"); no support terms or response-time commitment beyond "we answer the same day, most days"; no backup, RPO/RTO or disaster-recovery statement anywhere; no data residency or region; no retention periods; no breach-notification commitment; no complete subprocessor list (Anthropic is named well, AWS is not named at all despite being the host); no DPA or willingness to sign one; no governing law, jurisdiction, notice address or dispute clause in Terms; no exit terms — what happens to the record when the pilot ends, given that bulk export is roadmap and deletion is refused by design. The trust page is the best-executed part of this site and it stops precisely where a procurement questionnaire starts.
  FIX: Add one trust group, "Operations and continuity", with honest entries — most of them "Not supported" or "Roadmap", which is fine and consistent with the page's voice. Name AWS as the hosting subprocessor and give the region. Add governing law and a notice address to Terms. Add one line to the pilot page: what happens to the record at the end of the term, and by what mechanism it is returned.

--- [P2] /pilot's meta description advertises two sections the page does not contain
  WHERE: src/lib/routes.ts:48-55 vs src/app/pilot/page.tsx
  routes.ts describes /pilot as: "A planned Fall 2026 pilot with Simon's Office of Student Engagement: scope, who does what, onboarding inputs, support model and how success is measured." The page has scope, inclusions, audience and a four-step timeline. It has no support model and no success criteria — nothing on the page says what would count as the pilot having worked, which is the question I care most about, since I would be the one signing for it.
  FIX: Add the two missing sections rather than trimming the description — "How we support you" (who responds, how fast, what escalation looks like for two founders) and "How we'll know it worked" (three named, measurable criteria agreed before the term starts). A pilot page with no success definition reads as a pilot with no exit.

--- [P3] "Contact Sales" still appears in the footer and the FAQ after site.ts retired the phrase
  WHERE: src/components/site/SiteFooter.tsx:45 and src/components/home/Faq.tsx:93-95
  site.ts:23-27 states the rule: "One conversion phrase, used everywhere. 'Contact Sales' oversold a two-founder company and set the wrong expectation for who picks up" — and sets ctaLabel to "Book a walkthrough". The site-wide footer link reads "Contact Sales" and the FAQ closer reads "Contact sales". The footer is on every page, so the retired phrase is the most-rendered CTA text on the site. Minor on its own; it lands as one more small gap between the stated discipline and the shipped result.
  FIX: Let both call sites default to site.ctaLabel by dropping the children override, and add a claims-suite assertion that the string "contact sales" never appears in rendered text.

--- [P3] The Calendly embed is hardcoded to a white background, which breaks in the dark theme
  WHERE: src/components/site/Scheduler.tsx:33-36
  Scheduler passes background_color=ffffff to initInlineWidget. The site ships a real dark theme with an explicit toggle, so a visitor in dark mode who expands the scheduler gets a 680px white slab under a dark page. The surrounding component is otherwise the most carefully engineered thing on the site — plain-anchor fallback, honest failure state, nothing third-party loading before intent — which makes the one hardcoded colour stand out.
  FIX: Read the resolved theme at click time and pass background_color/text_color to match, or drop both parameters and let Calendly use its own default. primary_color=116d45 can stay; it is theme-independent.

========================================================================================
## Sophomore, elected treasurer of a student club three weeks ago. Inherited a Google Drive folder and one spreadsheet. Nobody explained anything. Someone in the club said "we use Tenure now" and sent me the link. I am reading this on my phone between classes. I did not choose this and I am not buying anything — I just need to know what it is and what I'm supposed to do.
========================================================================================
FIRST TRUST FAILURE: The /pilot page. Its intro says "Scope and timing are proposed, not contracted", and twenty-five lines later it says "No waiting list, no application, and no placeholder accounts. If OSE stewards your organization, you're in." Both cannot be true. It is the only sentence on the whole site addressed to me, and once I noticed the contradiction I started reading everything else as sales copy — including the trust strip, which turns out to be the one place still saying "Immutable audit trail" when every other section on the site was corrected to "append-only".
FIRST COMPREHENSION FAILURE: The line directly under the headline: "Tenure is the system of record for organizations where people rotate faster than knowledge transfers." I stopped there. I don't know what a system of record is, and "rotate faster than knowledge transfers" is not how anyone talks. I only understood the product 34 words later, at "the next leader opens a record instead of an empty folder" — because I am the next leader and I am staring at the empty folder.
FIRST BLOCKED ACTION: Getting into Tenure. I was told the club uses it now, and the only buttons anywhere on the site are "Book a walkthrough" (header, hero, every page footer band) and "Contact Sales" (footer, FAQ) — all of which lead to a 30-minute screen-shared call with the founders. There is no sign-in, no "your office set this up, ask your advisor", nothing that acknowledges a person who already has an account. I closed the tab without a next step.
WOULD NOT SURVIVE A DEMO: "26 organizations modelled", displayed under the heading "Continuity, measured / We measure whether the knowledge survived" and the line "Every number here is counted, not projected." The first question in any live room is "so twenty-six clubs use this?" and the answer is no — they are seeded rows from a roster file, which the register (C-014) says must never be presented as customers, users or adoption. The 0.86rem sub-line carries the qualifier; the 3.6rem numeral carries the impression. Second: the hero's "policy v4 snapshot" — ask what version 4 was and there is no answer, because policySnapshot is never read back, which is why your own EVIDENCE-REPORT.md:84 already records this string as removed. It is still on the page.

--- [P0] /pilot tells me I am already enrolled in a pilot that nobody has signed
  WHERE: C:\Users\adiab\tenure-landing\src\app\pilot\page.tsx:211-216 (vs the same page's intro at :176-184)
  The page intro says "Scope and timing are proposed, not contracted." Twenty-five lines later the same page says: "One term, hands-on the whole way. No waiting list, no application, and no placeholder accounts. If OSE stewards your organization, you're in." Both cannot be true. C-021 (src/lib/claims.ts:588-590) marks the pilot blocked-external and forbids "any implication of completed procurement"; the evidence line is "Verbal agreement only... NO written or signed commitment exists." None of the forbiddenPilotPhrases regexes match this sentence, so the ratchet passes it. This is the only sentence on the entire site written directly to a student officer, and it is a promise the company cannot keep — and it is published where the unsigned office can read it.
  FIX: Delete "No waiting list, no application, and no placeholder accounts" and rewrite the last clause to "If OSE stewards your organization, the proposed scope includes you." Add /\byou'?re in\b/i to forbiddenPilotPhrases so it cannot come back.

--- [P1] The hero still displays "policy v4 snapshot" — an artifact your own evidence report says was deleted
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\HeroFloatingCards.tsx:60
  The floating card over the hero dashboard reads "Spring Gala budget, $4,200. policy v4 snapshot". C-005's qualification (claims.ts:130-131) states the stored policySnapshot "is a thin flag object and is never read back. Do NOT claim the product can 'prove the rules in force' or reconstruct a policy version." docs/CLAIMS-AUDIT.md:59 flagged this exact string and proposed "VP Finance · gate 2 of 2"; docs/EVIDENCE-REPORT.md:84 records it as "Removed — an invented artifact." The twin instance in DashboardMock was fixed (now ":250 decided by VP Finance & Operations · append-only step"); this one was missed. The ledger says it is gone. It is not gone, and it is on the home page hero.
  FIX: Apply the fix the audit already wrote: replace "policy v4 snapshot" with "VP Finance · gate 2 of 2". Then correct EVIDENCE-REPORT.md:84, which currently records a removal that did not happen.

--- [P1] "Ask anything" ships inside the hero, in the one place the regex can't see it
  WHERE: C:\Users\adiab\tenure-landing\src\components\visuals\DashboardMock.tsx:452
  The AI panel of the hero dashboard renders "Ask anything about the finance this seat has handled." C-007's qualification (claims.ts:391) says: "Retrieval is keyword matching over five record kinds — no embeddings, no vector search, document file contents not indexed. Never say 'instant', 'never invents' or 'answers anything'." SeatMechanism.tsx:265 even carries the comment "Not 'ask anything': C-007 limits retrieval to five record kinds" — the team knew. The forbidden regex is /\banswers? anything\b/i, which does not match "Ask anything", so claims.spec.ts is green. Ground truth also says finance figures and people are not in the AI corpus, so "ask anything about the finance" is the most wrong possible instance of it.
  FIX: Change to "Ask about the {module} this seat has recorded." and add { phrase: /\bask (it |me )?anything\b/i, because: "Retrieval is keyword matching over five record kinds", claimId: "C-007" } to forbiddenPhrases.

--- [P1] There is no path for the person who already has Tenure — which is me
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\Hero.tsx:66-73; SiteHeader.tsx:127; SiteFooter.tsx:44-52; CtaBand.tsx:56-62
  Every call to action on every page is the same one: "Book a walkthrough" (Hero, header, CtaBand, PageHeader, /pilot ×2) or "Contact Sales" (footer, FAQ), all routing to /contact, which offers a 30-minute screen-shared founder call and an email address. I was told my club uses this. I am not booking a sales call. A repo-wide grep for sign in / log in / get started returns nothing but a footer heading and a Terms sentence. The product's entire thesis is that the incoming officer is the beneficiary, and the site has nothing for an incoming officer to do. I understand you cannot publish the sign-in mechanism (C-023 forbids it) — but silence reads as "this site is not for you", and that is exactly how it reads.
  FIX: Add one line to the footer's "Get started" column: "Already using Tenure? Accounts are set up by your office — ask your advisor or email hello@tenurework.com." No mechanism named, no C-023 exposure, and the actual user stops falling off the page.

--- [P1] The first paragraph spends 34 words before saying anything I can picture
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\Hero.tsx:45-47 and :57-63
  "Tenure is the system of record for organizations where people rotate faster than knowledge transfers. Finance, events, approvals, members and memory attach to the seat, not the person holding it — so the next leader opens a record instead of an empty folder." I stopped at "system of record" — I don't know what that is. "Rotate faster than knowledge transfers" is not a sentence anyone says out loud. "Attach to the seat" means nothing yet, because "seat" hasn't been defined and won't be for two more sections. The clause that actually landed is the last one — "the next leader opens a record instead of an empty folder" — because I am the next leader and I am looking at the empty folder right now. It is the 34th word. Above it, the label "The operating system for organizational memory" adds a third abstraction before the headline.
  FIX: Invert it: "When your club's leaders change, the next one usually inherits a folder and a guess. Tenure keeps the money, contacts, deals and decisions attached to the role instead of the person — so the next treasurer opens a record, not an empty folder." Same claims, no undefined nouns in the first clause.

--- [P1] "Productive in days, not a semester" is the unmeasured claim the home page deliberately deleted
  WHERE: C:\Users\adiab\tenure-landing\src\app\product\page.tsx:213-215; C:\Users\adiab\tenure-landing\src\app\pilot\page.tsx:161
  Problem.tsx:15-17 carries the comment: "'The next leader is productive in days' was an outcome nothing measures; shadow access before the term starts is the mechanism that was meant by it." MetricsBand.tsx:39-42 records the same deletion. But the claim is still an H2 on /product — "Productive in days, not a semester." — and restated on /pilot as "productive from the very first meeting." docs/CLAIMS-AUDIT.md:135 is explicit: "nothing in either repo measures onboarding duration — no telemetry, no event, no report computing time-to-productive, and the pilot has not run." The two survivors also disagree with each other (days vs. first meeting) while the home page claims neither.
  FIX: Replace the /product H2 with the mechanism the audit endorsed — "They read the seat before they sit in it." — and end the /pilot step at "...answers from the seat." Then add a claims.spec rule for /\bproductive (in|from)\b/i so the third instance can't reappear.

--- [P2] The one FAQ answer opened for me by default is about query-layer database isolation
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\Faq.tsx:71 (open={i === 2}); answer at :33-41
  The accordion hardcodes open={i === 2}, which is "Is sensitive data handled responsibly?" — whose answer begins "Isolation is enforced at the query layer by the database client itself, and privileged actions append to a create-only audit trail..." On a phone, that is a wall of infrastructure vocabulary sitting pre-expanded, and it is the last content block before the closing sales band. Meanwhile the question I actually have — "Does Tenure replace the tools we already use?" — is collapsed. The choice tells me precisely who this page thinks is reading it, and it isn't me.
  FIX: Open i === 0. The security answer stays one tap away for the reviewer who wants it, and /trust already exists for that reader.

--- [P2] Section two of the home page is four credentials written for a security reviewer, and one of them has no subject
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\TrustStrip.tsx:9-14
  Immediately under the hero, before anything has been explained, the trust strip shows four chips: "Proposed across the office's organizations", "Hard + soft conflict detection", "2-gate approval chain, 7 request types", "Immutable audit trail". Chip one has no noun — proposed WHAT? (It is the pilot scope, but detached from site.pilot.scope it is a fragment.) Chips two through four are vendor-evaluation vocabulary: I do not know what a soft conflict is, what a gate is, or what an audit trail does for me. On a phone these wrap into a grey block under two university logos, and it is the moment I would have closed the tab.
  FIX: Make the chips outcomes, not features: "Proposed pilot scope: every org the office stewards", "Catches double-booked rooms before you publish", "Every approval names the seat that made it", "Nothing is ever deleted". Same underlying claims (C-006, C-004, C-011), readable by the person the product is for.

--- [P2] "Immutable audit trail" is the one place the site still says immutable
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\TrustStrip.tsx:13
  Every other surface was corrected to the honest phrasing — Platform.tsx:262 "Append-only audit trail", Faq.tsx:35 "create-only audit trail", OfficeConsole.tsx:114 "append-only audit row". The trust strip chip still says "Immutable". C-004 and C-031 both instruct "never say hash-chained, tamper-proof, cryptographically immutable or WORM", and DEVELOPMENT-BIBLE-v1.0.md:294 repeats it. Ground truth: append-only is application convention, with no hash chain and 49/63 action coverage. The word survived because docs/CLAIMS-AUDIT.md:43 waved it through as a title, and because no forbiddenPhrases regex covers bare "immutable".
  FIX: Change the chip to "Append-only audit trail" and add { phrase: /\bimmutable\b/i, because: "Append-only is application-enforced, not cryptographic", claimId: "C-031" } to forbiddenPhrases.

--- [P2] "Knowledge cards" is load-bearing on the home page and is never defined on it
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\Handoff.tsx:209 and :242-244
  The handoff packet's fourth column is headed "Cards" and shows 22 / 34 / 19 / 27. I have no idea what a card is, so I cannot tell whether 34 is a lot or whether a seat with 19 is in trouble — and this table is presented as the proof of the whole product. The term recurs unexplained in SeatMechanism.tsx:54 ("knowledge cards included"), Handoff.tsx:124 ("including the knowledge cards counted above") and Faq.tsx:27. The only definition anywhere is on /trust:159, three clicks away, inside a paragraph about retrieval. Platform.tsx:216 lists the six card types as bare mono chips (Contact, Playbook, Vendor, Lesson, Credential, Deadline) without saying that is what a card is.
  FIX: Rename the column "Things it knows" or add one line under the packet header: "A card is one thing the seat learned — a vendor, a contact, a playbook, a lesson." Twelve words, and the entire table becomes readable.

--- [P2] The shadow/active/alumni lifecycle is told four times, and two adjacent sections carry the identical headline
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\Handoff.tsx:283-323 vs C:\Users\adiab\tenure-landing\src\components\home\AiOnboarding.tsx:77-88
  Handoff.tsx:287-289 heads its shadow-access block "They read the seat before they sit in it." The very next section, AiOnboarding.tsx:77-79, heads itself "They read the seat before they sit in it." — the same sentence, back to back, both then explaining read-only access before the term begins. The same lifecycle is also the three bullets at SeatMechanism.tsx:53-57 plus its occupant widget, and again at Platform.tsx:227-231 ("read-only before the term, revoked after it, record kept"). page.tsx:20-24 documents the refactor's whole purpose as fixing "Four arguments were made twice." This one is now made four times.
  FIX: Cut the shadow-access block from Handoff (SeatMechanism already owns the lifecycle) and let AiOnboarding keep the headline — or keep Handoff's and retitle AiOnboarding around what is actually new there, the asking.

--- [P2] "Continuity, measured" sits above four numbers, none of which measures continuity
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\MetricsBand.tsx:68-75 and C:\Users\adiab\tenure-landing\src\lib\site.ts:120-149
  The band promises "We don't measure engagement. We measure whether the knowledge survived" and "Every number here is counted, not projected." The four numbers are: 26 organizations modelled, 2-gate approval chain, 0 records deleted, 132 end-to-end tests. None measures knowledge survival — three are structural facts about the codebase and one (132 e2e tests) is engineering proof I have no use for. "26 organizations modelled" reads as adoption on a phone where the sub-line is 0.86rem grey; the honest qualifier ("structural counts from the seeded model, not a count of active users", C-014) is in the register, not in the tile. "0 records deleted" is trivially true when nobody has offboarded yet.
  FIX: Retitle the band "What we can show you today" (drop "measured" and "counted, not projected"), move the 132/292 test count to /trust where a reviewer wants it, and put the word "modelled" in the 26 tile's LABEL rather than only the sub-line.

--- [P2] On a phone the Platform section is twelve stacked cards and about twenty-five mono chips
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\Platform.tsx:105 (grid) and :19-25 (cluster chips)
  The grid is grid-cols-1 on mobile, so all twelve feature cells stack: Tenure AI, Approvals, Finance, Calendar, Memory, Members, Documents, Audit, Messages, Collaboration, Reports. Their tag rows add roughly 25 uppercase mono chips ("event, budget, vendor, comms, document, roster, exception", "versioned, edit in place, save-conflict check", "DM, board channel, approval thread, broadcast", "board-ready, seat-scoped search"). Each card's header also carries an unexplained cluster label — Remember / Govern / Run — that appears nowhere else on the page. This is the middle of the page and it is where a phone reader gives up scrolling. It is also the section the hero's secondary button ("Explore the platform", href="#platform") jumps you straight into, past both sections that would have explained what a seat is.
  FIX: Show six cards on mobile with the remainder behind "See all eleven modules →" to /product, drop the Remember/Govern/Run chips (they explain nothing without a legend), and point the hero's secondary button at #problem or the seat section instead.

--- [P2] The smallest type on the site exists only on phones
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\Handoff.tsx:146-152 (ColumnLabel, md:hidden, 0.55rem); 79 declarations ≤0.6rem across src/components/home and src/components/visuals
  Handoff's ColumnLabel is the mobile-only affordance that labels each cell once the table collapses — and it renders at 0.55rem (~8.8px), uppercase mono at 0.14em tracking, hidden from md up. The stacked table also puts the alumni contact email (maya.chen@alum.rochester.edu) and the seat code at 0.6rem (~9.6px) in text-text-secondary. Across the home and visuals components there are 79 declarations at ≤0.6rem, bottoming out at 0.5rem (8px) in SeatMechanism.tsx:253. The site's own label-mono utility is 0.6875rem, so these are deliberate overrides below the system's own floor. I am reading this outdoors on a phone; I did not read any of it.
  FIX: Floor any type visible at the mobile breakpoint at 0.7rem (11.2px), starting with ColumnLabel and the packet's seat-code/email lines. Sub-0.6rem is defensible in a desktop-only mock; it is not defensible in the mobile-only fallback.

--- [P2] Two product claims on the home page have no row in the claims register at all
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\TrustStrip.tsx:11 and Platform.tsx:186-203 vs C:\Users\adiab\tenure-landing\src\lib\claims.ts
  "Hard + soft conflict detection" is chip two of the trust strip, and Platform.tsx:191-200 states "Conflict-aware calendar / Hard and soft conflicts caught before publishing" with a rendered example ("Hard conflict, Schlegel 207, 5:00 to 6:30p"). Grepping src/lib/claims.ts for "conflict" returns only C-010's save-conflict check on documents — a different feature. The same is true of "Cross-org collaboration", "Messages" and "Reports & search". claims.ts:3-5 states the contract: "Every material claim the public site makes about the product... has a row here." The ratchet can only enforce rows that exist, so its green build is weaker evidence than the header comment implies.
  FIX: Add register rows (with evidence commits) for conflict detection, messaging and cross-org collaboration, or drop conflict detection from the trust strip until one exists — a trust chip is the last place to put an unregistered claim.

--- [P3] "Contact Sales" survives in two places despite site.ts declaring one conversion phrase
  WHERE: C:\Users\adiab\tenure-landing\src\components\site\SiteFooter.tsx:45 and C:\Users\adiab\tenure-landing\src\components\home\Faq.tsx:93-95
  site.ts:23-27 states: "One conversion phrase, used everywhere. 'Contact Sales' oversold a two-founder company and set the wrong expectation for who picks up" — and sets ctaLabel to "Book a walkthrough". The footer then hardcodes "Contact Sales" and the FAQ hardcodes "Contact sales", in two different casings, both overriding the ContactSalesLink default. To me, "Contact Sales" is a sign that the page is not for me; that is exactly the reaction the comment was written to prevent.
  FIX: Drop both children so they inherit site.ctaLabel, and rename the components ContactSales/ContactSalesLink to BookWalkthrough so the old label has nowhere to hide.

--- [P3] The cost answer tells the treasurer nothing a treasurer can use
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\Faq.tsx:44-46
  "Pricing is per portfolio, not per club: one budget line for every organization an office stewards." Managing a budget is literally my job, and this answer does not tell me a number, a range, or — the only thing I actually need — whether my club pays anything. Two sentences would fix it and it would still commit to no figure.
  FIX: Append: "Individual clubs are not billed — the office funds the portfolio. Nothing comes out of your club's budget." (Only if true; if it isn't decided, say "we're still setting pricing for the pilot" instead of implying it's settled.)

--- [P3] Vendor logos anchor a section whose own register row says a file import is not an integration
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\Integrations.tsx:14, :22-23, :31
  Each Integrations card leads with a logo chip: Excel, Outlook, Google Calendar. C-029's qualification (claims.ts:519-520) reads: "No vendor logo may appear on the site unless connector code and an end-to-end test exist. Importing a file a vendor produced is not an integration." Excel has no connector — it is XLSX parsing (C-012). The prose beneath is unusually honest ("no accounts to connect", "One-way today: Tenure fills your calendar and never reads it back"), which is what keeps this at P3, but the logos are the first thing the eye lands on and they say "integrations" before the prose says otherwise.
  FIX: Either drop the Excel chip (the card title already names the formats) or amend C-029's qualification to permit format marks explicitly, so the rule and the site stop disagreeing.

========================================================================================
## University IT security reviewer (institutional risk, tenancy, authN, audit, subprocessors, incident response, vendor exit)
========================================================================================
FIRST TRUST FAILURE: The home page's trust strip chip "Immutable audit trail" (src/components/home/TrustStrip.tsx:13), read two minutes before /trust told me there is no hash chain, no signature, no checksum, and that "Any vendor claiming immutability should be asked which of those three it means." The trust page pre-emptively warns me about the claim its own home page makes.
FIRST COMPREHENSION FAILURE: How a human being authenticates to Tenure. The site says SSO is roadmap and that "accounts are provisioned by us in advance," which describes provisioning, not authentication. There is no word anywhere on the site about passwords, MFA, session lifetime, lockout, or account recovery — while the site's own claims register (src/lib/claims.ts:364) records AUTH_DEV_LOGIN=true and ALLOW_DEV_LOGIN_IN_PRODUCTION=true in the production task definition and then instructs the site not to publish the sign-in mechanism.
FIRST BLOCKED ACTION: Building a risk-register row for the vendor: I could not find a subprocessor list, a hosting provider, a data region, a DPA, or any document I could attach. The only named third party on the entire site is Anthropic; AWS appears once, incidentally, inside an encryption limit ("Keys are AWS-managed"), and no region or residency statement exists anywhere.
WOULD NOT SURVIVE A DEMO: The flagship "Ask this seat" exchange on the home page (src/components/home/SeatMechanism.tsx:269-274): "Who's our caterer, and what did we overpay last year?" answered with a vendor, a person, and two dollar figures. /trust:161 states that finance figures and people records are not in the AI corpus, "so those questions cannot be answered by the assistant today." Same for the "Bylaws §4" citation shown twice (Platform.tsx:126, AiOnboarding.tsx:28) — /trust:161 says document file contents are not indexed, only titles and descriptions. Typing either question on a live screen-share produces nothing.

--- [P0] Home page calls the audit trail "Immutable" — the trust page says that word is a warning sign
  WHERE: src/components/home/TrustStrip.tsx:13
  src/components/home/TrustStrip.tsx:13 renders a chip reading "Immutable audit trail" in the orientation strip directly under the hero. /trust:96-101 states the audit table has no hash chain, signature or checksum column, that append-only is enforced by the application only, and — verbatim — "Any vendor claiming immutability should be asked which of those three it means." The claims register (claims.ts:278, 350) forbids "cryptographically immutable" and the development bible repeats it (docs/DEVELOPMENT-BIBLE-v1.0.md:294). The automated ratchet does not catch it because forbiddenPhrases only blocks /tamper-proof/ and /hash-chain(ed)/, never /immutable/. Coverage is also 49 of 63 server actions, so it is neither immutable nor complete.
  FIX: Change the chip to "Append-only audit trail" (the phrasing /trust and Platform.tsx already use), and add { phrase: /\bimmutab(le|ility)\b/i, claimId: "C-031" } to forbiddenPhrases in src/lib/claims.ts so it cannot come back.

--- [P0] /story states the uncontracted pilot as an accomplished fact, in the section titled "Early, and honest about it"
  WHERE: src/app/story/page.tsx:188-191 and src/app/pilot/page.tsx:210-214
  src/app/story/page.tsx:187-192 reads: "Here is exactly where things stand... Every organization Simon's Office of Student Engagement stewards puts Tenure to work, and so does the office itself." Present tense, no hedge, in the paragraph that promises exactness. C-021 records the Fall 2026 pilot as verbally agreed with NO written commitment, and forbids "any implication of completed procurement or university endorsement." /pilot:210-214 compounds it: "No waiting list, no application, and no placeholder accounts. If OSE stewards your organization, you're in" — that asserts the office has already enrolled its portfolio, four lines after the page header says "Scope and timing are proposed, not contracted." Both evade the claims ratchet: the hedge test (claims.spec.ts:506) only fires on sentences containing BOTH the office name and the literal string "Fall 2026", and these say "this fall" and "OSE". A university that finds a vendor describing its office this way escalates to brand/counsel, not to procurement.
  FIX: Story: "This fall, we are planning to run Tenure with every organization Simon's Office of Student Engagement stewards, and with the office itself. Scope is proposed, not contracted." Pilot: replace "you're in" with "the proposed scope includes you." Then add a ratchet rule: any sentence naming the office plus a present-tense verb of adoption (puts/runs/uses/is on) with no HEDGES match fails.

--- [P0] Every AI demonstration on the site shows an answer the retrieval layer provably cannot produce
  WHERE: src/components/home/SeatMechanism.tsx:269-274; src/components/home/Platform.tsx:126; src/components/home/AiOnboarding.tsx:28; src/app/product/page.tsx:82
  Four places show Tenure AI answering outside its own corpus. (1) SeatMechanism.tsx:269-274 — a caterer/overspend question answered with a person and two dollar figures; /trust:161 says finance figures and people records are not in the corpus. (2) Platform.tsx:126 and (3) AiOnboarding.tsx:28 — an elections answer cited to "Bylaws §4"; /trust:161 says document file contents are not indexed, only titles and descriptions, and C-020's qualification states policies are NOT in the AI corpus and "Do not imply Tenure AI can answer policy questions." (4) product/page.tsx:82 — "Tenure AI answers from the seat's full record", where the record is five keyword-matched kinds with finance, people and file contents excluded. The claims ratchet checks for forbidden words, not for whether a rendered mock is answerable — so the most persuasive artefact on the site is the least verified.
  FIX: Rewrite all four to questions the five record kinds can actually serve — knowledge cards, approvals, events, document titles, org records. E.g. "Who did we use for catering, and what did the last board decide about it?" → answered from a vendor knowledge card plus the approval, cited "2 records". Replace "Bylaws §4" with a knowledge-card source. Change product/page.tsx:82 "full record" to "the seat's recorded knowledge". Add a claims.spec assertion that every rendered source label matches one of the five kinds.

--- [P0] /trust documents no authentication control at all — the one question every security review opens with
  WHERE: src/app/trust/page.tsx:174-180 (absent control); src/lib/claims.ts:363-369 (the withheld evidence)
  The "Identity and integrations" group (trust/page.tsx:174-180) says SSO is not deployed and "accounts are provisioned by us in advance and the environment is not open to public registration." That is a provisioning statement dressed as a control. Nowhere on the site is there a password policy, MFA position, session lifetime, lockout threshold, or account-recovery process. Meanwhile the site's own register (claims.ts:363-369) cites ecs.tf:167-168 with AUTH_DEV_LOGIN=true and ALLOW_DEV_LOGIN_IN_PRODUCTION=true, and then instructs: "Do not publish the specifics of the pilot sign-in mechanism." A page whose entire premise is "what is actually built, and what is not" has made a deliberate decision not to disclose the control that gates everything else on it. I will find those two flags in ten minutes of a real assessment, and at that point every other honest disclosure on the page is retroactively suspect.
  FIX: Add an "Authentication" control under Identity, stated to the same standard as the rest of the page: the mechanism in use for the pilot, whether MFA exists (it does not — say so, status "unsupported"), session lifetime, and whether any development sign-in path is reachable in the production environment. If the honest answer is that a dev-login flag is enabled in production, that is a product fix before it is a copy fix — but the page must not stay silent while the register knows.

--- [P0] There is no legal entity to sign anything, and /pilot invites institutions in without saying so
  WHERE: src/app/pilot/page.tsx (whole page, esp. 210-214) vs src/app/privacy/page.tsx:22-30
  /privacy:22-30 and /terms:24-31 disclose honestly that Tenure is not incorporated and that "Tenure" is a trading name for two individuals (C-032, ledger blocker). That disclosure is buried in the legal footer. /pilot sells a full-portfolio engagement covering student finance, rosters and documents, with "no application" and "you're in", and never mentions that there is no counterparty for a data protection agreement, no FERPA school-official written agreement, no certificate of insurance, no HECVAT, and no indemnitor. A university cannot lawfully place education records with a vendor under the FERPA school-official exception without a written agreement establishing direct control — and there is no legal person to execute one with. /trust:317-335 says the privacy notice and terms are founder drafts not reviewed by counsel; that is candour, not a control.
  FIX: Put the contracting position where the buyer is, not only in the legal footer: a short "What you would sign" block on /pilot and a "Contracting and legal entity" control on /trust, with status roadmap, stating plainly that incorporation and a counsel-reviewed DPA precede any handling of institutional records, and naming the target date.

--- [P1] The tenant-isolation control's headline is contradicted by its own limit for 24 of 39 models
  WHERE: src/app/trust/page.tsx:26-34
  trust/page.tsx:26-27 promises "separated at the database-client layer, not by convention in each query," and the body at :32 says "an individual query cannot decline it." The limit at :34 then discloses that only 15 of 39 models carry a tenant column; "the remainder are reached through their parent relation and are recorded in the tenancy registry as not independently enforceable at the query layer." For those 24 models, the Prisma extension has no column to filter on, so isolation depends entirely on every call site traversing the parent relation — which is precisely the "convention in each query" the blurb says Tenure does not rely on. The control is badged "Verified in CI" over the whole group. This is the strongest disclosure on the page and it still overstates: the absolute sentence and the limit cannot both be true.
  FIX: Scope the headline sentence: "For the 15 models carrying a tenant column, an individual query cannot decline the filter. The remaining 24 are reached through a parent relation, where isolation depends on the query traversing that relation — the registry records which is which." And state what CI asserts for the 24, if anything.

--- [P1] The subprocessor disclosure is not usable for a risk register — one subprocessor named, no hosting provider, no region, no list
  WHERE: src/app/trust/page.tsx:143-148 and src/app/privacy/page.tsx:95-99
  trust/page.tsx:145 says "Anthropic is the only model subprocessor" — precise, but a scanning reader takes it as "the only subprocessor," and nothing on the page corrects that. AWS is never named as a processor; it appears once, incidentally, at :113 ("Keys are AWS-managed"). /privacy:95-99 offers "Service providers we rely on... for example, hosting, infrastructure, and the AI provider" — "for example" is not a list. Absent entirely from the whole site: data residency or AWS region, whether records ever leave the US, which Anthropic API region serves the calls, retention by Anthropic, Calendly (which processes prospect names and emails on /contact), any mail provider, and any commitment to notify customers before a subprocessor changes. Grep across src/app, src/components and src/lib returns zero occurrences of "region", "residency", or "sub-processor" outside the single Anthropic sentence.
  FIX: Add a subprocessor table to /trust: name, purpose, data categories, processing location/region, and change-notification commitment — Anthropic (answer synthesis, retrieved record text), AWS (hosting, database, object storage, named region), Calendly (scheduling, marketing site only), plus any mail sender. Change "the only model subprocessor" to "the only model subprocessor; the full list is above."

--- [P1] No backup, restore, retention or in-transit statement anywhere — under a product whose entire promise is durability
  WHERE: src/app/trust/page.tsx:104-130 (group is silent); src/app/terms/page.tsx:117-125
  The "Data handling" group (trust/page.tsx:104-130) covers exactly four things: encryption at rest, signed links, refusal to delete, and the absence of bulk export. It says nothing about backups, restore testing, RPO/RTO, retention periods, or encryption in transit. The site simultaneously sells permanence — "0 records deleted at offboarding" (site.ts:136-140), "History cannot be deleted... it is the property the whole product rests on" (trust:123). A durability promise with no stated backup or restore posture is an unbacked promise. Compounding it: there is no self-service export (trust:126-129, roadmap), no public API and no webhooks (trust:195), no incorporated entity, and two founders. /terms:117-125 offers only that the org can "ask us to close its account and return or delete its record" and that Tenure will give "a fair chance to retrieve its record where we reasonably can." There is therefore no answer on this site to "what happens to four years of our students' records if you two stop."
  FIX: Add three controls to Data handling with honest statuses: "Backups and restore" (frequency, retention, whether a restore has ever been tested — say "never tested" if so), "Encryption in transit", and "Retention schedule". Add a "Continuity" control stating what an institution gets if Tenure stops: who holds the data, in what format, within what window, and whether a database dump on request is committed.

--- [P1] No breach-notification commitment — the security section covers inbound reports only
  WHERE: src/app/trust/page.tsx:296-311 and src/app/privacy/page.tsx:136-142
  trust/page.tsx:296-311 ("Reporting a security issue") tells me how to report a problem TO the founders and promises they will confirm receipt and not argue about severity. There is nothing anywhere on the site about the reverse direction: no commitment to notify the institution of a breach, no timeframe, no threshold, no named contact, no forensics or preservation commitment. University counsel in New York will require contractual notice under the SHIELD Act, and every HECVAT and DPA I process has a notification clause with hours in it. /privacy:136-142 offers only "reasonable safeguards" and "we can't promise absolute security."
  FIX: Add "Incident notification" beside the reporting block: the commitment (notify the affected institution's named contact without undue delay and no later than X hours of confirming an incident), what the notice will contain, and — honestly — that there is no 24/7 on-call and no formal IR runbook today if that is the case.

--- [P1] /privacy promises to honour deletion requests; /trust says the product is built to refuse deletion
  WHERE: src/app/privacy/page.tsx:144-150 vs src/app/trust/page.tsx:121-124
  privacy/page.tsx:144-150: "You can request access to the information we hold about you, ask for an export, or request deletion... we'll work with you and your organization to honor the request." trust/page.tsx:121-124: "History cannot be deleted. A seat carrying assignments, holdings or knowledge is refused deletion and must be retired instead... This is deliberate: it is the property the whole product rests on." Both are true and they cannot both be honoured. A graduating student asking for erasure of their name from an approval chain hits a mechanism designed to refuse. Neither page reconciles this, and neither states a retention period after which anything is removed. This is the question a student privacy officer asks, and the answer currently depends on which page they read.
  FIX: Say what actually happens: individual account and contact data can be removed on request; entries in the organizational record (approvals, decisions, seat history) are retained as the organization's record and, at most, the individual's identifiers are redacted. State the retention period for the organizational record and who the controller is for it — the institution, not Tenure.

--- [P1] The site advertises that its seeded data is a real university roster, and offers to screen-share "a real organization's record" with any prospect
  WHERE: src/lib/site.ts:120-126 and src/app/contact/page.tsx:16 (ledger X-01, docs/LANDING-REFINEMENT-LEDGER.md)
  site.ts:120-126 sells the headline metric as "26 organizations modelled — 209 seats, built from the office's own leadership roster rather than invented for a demo," and C-014's evidence cites roster-source.mjs. contact/page.tsx:16 then promises every prospect: "We open a real organization's record — seats, approvals, the budget, the handoff packet — and you ask what you want to see." Read together, the site tells me it will show real University of Rochester student roster data to arbitrary strangers who book a Calendly slot, from an office that has signed nothing. Separately, the project's own ledger records unresolved item X-01: "Student roster in public git history" in satvikOS/Tenure, needing "History rewrite or private repo." The marketing copy is effectively a signpost to that exposure.
  FIX: Change contact/page.tsx:16 to "We open a demonstration workspace built on the same model" and confirm the walkthrough environment carries no real student names. Change the metric subtitle to "modelled on the structure of a real portfolio" — the structural point survives without asserting that real people's records are loaded. Resolve X-01 before either sentence goes back.

--- [P1] /contact suppresses Calendly's consent banner while the site has no website-visitor privacy or cookie disclosure
  WHERE: src/components/site/Scheduler.tsx:34; src/lib/calendly.ts:13-14; src/app/privacy/page.tsx (no visitor/cookie section)
  Scheduler.tsx:34 initialises the widget with hide_gdpr_banner=1, which turns off the third-party consent notice Calendly would otherwise show, while Calendly's script, stylesheet and cookies load into the page (lib/calendly.ts:13-14, and the widened CSP in next.config.ts:42-55). The privacy notice describes only product data — account details, organizational records, "basic usage data" — and never mentions website visitors, cookies, Calendly, or what a prospect's name, email and meeting notes are used for. next.config.ts:16 justifies the CSP posture with "there is no user data to protect", which is the one route where that is untrue.
  FIX: Drop hide_gdpr_banner=1, or add a short "This website" section to /privacy naming Calendly, the cookies it sets, what booking data it collects, and confirming no analytics or tracking runs on the site (which is true today and worth saying).

--- [P2] /pilot implies advisors see only their own organizations — /trust says no institution account is scoped
  WHERE: src/app/pilot/page.tsx:126
  pilot/page.tsx:126: "Three staff levels, so an advisor sees advising and a director sees everything." To a reviewer that reads as data segmentation. trust/page.tsx:64-68 states the opposite as "Not supported": "Any account with an institution membership can currently read every organization in the portfolio. An advisor relation exists in the schema but does not narrow reads," and C-025's qualification adds "Do not claim 'least access by default'." The tiers are capability-nested, not org-scoped, and the pilot page is the page an OSE director reads before granting advisor accounts across 26 organizations' finances.
  FIX: "Three staff levels, so an advisor sees a narrower set of actions than a director. Note that staff accounts are not yet scoped per organization — see the trust page." Link it.

--- [P2] "Productive in days, not a semester" survives on /product after the same claim was stripped everywhere else as unmeasured
  WHERE: src/app/product/page.tsx:213-216
  product/page.tsx:213-216 headlines an outcome claim. The refinement pass explicitly removed this class of claim elsewhere and documented why: Problem.tsx:15-17 notes "'The next leader is productive in days' was an outcome nothing measures"; site.ts:110-118 says a 3-day onboarding counter "was theatre" because "there is no telemetry measuring onboarding time"; MetricsBand.tsx:39-42 records removing the struck-through comparison for the same reason. There is no claim ID behind this headline. It is the only unmeasured outcome promise left on the site, and it sits above a Q&A card demonstrating the AI capability.
  FIX: Use the mechanism, as the rest of the site now does: "They read the seat before they sit in it." — shadow access is the thing that is actually built and testable.

--- [P3] The claims ratchet has two holes that let the two worst claims through, and both are one-line fixes
  WHERE: src/lib/claims.ts:668-685 and e2e/claims.spec.ts:506-536
  claims.spec.ts is unusually good — it tests its own matcher, refuses to excuse bare assertions, and fails when /trust deletes a disclaimer. But forbiddenPhrases (claims.ts:668-685) has no rule for "immutable" even though the register's qualifications forbid it twice (claims.ts:278, 350), and the pilot hedge test (claims.spec.ts:506-536) only inspects sentences containing both the office name and the literal "Fall 2026", so /story's "this fall... puts Tenure to work" is invisible to it. Both P0 copy defects above are inside these two gaps. Also stale: the comment at claims.spec.ts:947-950 describes a FAQ question ("Does Tenure replace our Google Drive, Slack, or Notion?") that Faq.tsx no longer contains.
  FIX: Add /\bimmutab(le|ility)\b/i → C-031 to forbiddenPhrases; extend the pilot test to fail any sentence naming the office with a present-tense adoption verb (puts|runs|uses|is on|are on) and no HEDGES match, regardless of whether the season string appears; refresh the stale FAQ comment.

========================================================================================
## University procurement and contracts officer — reading /privacy and /terms with a red pen against a standard vendor-onboarding checklist (entity, signature authority, governing law, liability, indemnity, insurance, confidentiality, breach notice, subprocessors, retention, data return, wind-down, FERPA posture, accessibility).
========================================================================================
FIRST TRUST FAILURE: The chip "Immutable audit trail" on the home page (src/components/home/TrustStrip.tsx:13), read against the vendor's own /trust page, which says "Cryptographic tamper-evidence — NOT SUPPORTED. The audit table has no hash chain, signature or checksum column. Append-only is enforced by the application, not by cryptography or by write-once storage," and "Coverage is 49 of 63 server actions." Every other surface on this site uses the careful words "append-only" or "create-only." One place uses "Immutable," and it is the one place a reviewer skims first. Once I find a security claim the vendor's own documentation contradicts, I have to re-verify everything else they told me, including the parts I was inclined to credit.
FIRST COMPREHENSION FAILURE: Whether the Fall 2026 pilot costs anything. /terms governs "the pilot and early access" and never once mentions fees, payment, taxes, or that the pilot is no-cost. The only pricing statement on the entire site is the home FAQ (src/components/home/Faq.tsx:45): "Pricing is per portfolio, not per club: one budget line for every organization an office stewards" — no number, no unit, no statement about the pilot. I cannot tell whether this is a $0 evaluation that needs only a data agreement, or a purchase that needs a competitive-bid waiver. That single unknown determines which of two completely different approval paths I open.
FIRST BLOCKED ACTION: Opening a vendor file. I went to /terms to find the counterparty's legal name, entity type, state of formation and business address so I could request a W-9 and a certificate of insurance. /terms tells me only that "Tenure" is "a trading name for the two founders" — and unlike /privacy, the terms page does not even link to where those two people are named. There is no address anywhere on the site, no entity, no signature block, and no statement of who would sign. I cannot create a vendor record, cut a PO, or route anything to counsel.
WOULD NOT SURVIVE A DEMO: Two sentences. (1) /story: "Every organization Simon's Office of Student Engagement stewards puts Tenure to work, and so does the office itself." One phone call to OSE — who have verbally agreed to nothing more than a conversation and have signed nothing — and that sentence collapses, taking the credibility of the "Early, and honest about it" section it sits inside with it. (2) /contact: "We open a real organization's record — seats, approvals, the budget, the handoff packet." The first question in the demo is "whose record is this, and did they consent to you showing it to me?" If the answer is a real Simon club, the vendor has just demonstrated, live, that our record would be shown to their next prospect — and nothing in their Terms prevents it.

--- [P0] There is no legal person to contract with, and neither document says who would sign
  WHERE: src/app/terms/page.tsx:24-31; src/app/privacy/page.tsx:22-30; src/components/site/SiteFooter.tsx:72
  The banner on /privacy says "Tenure is not yet an incorporated entity — 'Tenure' below is a trading name for the two founders named on the story page." The same banner on /terms drops even the cross-reference: "a trading name for the two founders," unnamed and unlinked. Disclosing this is correct and I credit it. But disclosure is not the deliverable. Two people carrying on a business together are, by default, a general partnership — which state's law, at which address, is nowhere on this site. There is no entity name, no EIN, no registered agent, no business address, no signature block, and no statement of who the counterparty would be. Meanwhile src/components/site/SiteFooter.tsx:72 asserts "© 2026 Tenure. All rights reserved." — a rights notice in the name of a legal person the vendor has just told me does not exist. Every downstream item on my checklist (W-9, certificate of insurance, conflict-of-interest disclosure, vendor registration, wire instructions) is blocked at step one. The banner's instruction — "ask us for the reviewed version before signing anything" — puts the burden on me to discover what does not exist.
  FIX: State the counterparty explicitly in both banners: "Any agreement would be executed by Almamy Diaby and Satvik Adyanthaya personally, as a general partnership under the law of New York, unless an entity is formed first." Add a mailing address. Add one line naming what will change before signature (entity formation, EIN, counsel review, insurance). Change the footer to "© 2026 the Tenure founders" or drop the reserved-rights formula until an entity holds the rights.

--- [P0] Unilateral amendment with deemed acceptance — the sentence I will not sign under
  WHERE: src/app/terms/page.tsx:40-47 and 127-133
  /terms, "Changes to these terms": "When we make a meaningful change, we'll update the date above and let active organizations know. Continuing to use the service after a change means you accept the updated terms." Compounding it, "Acceptance of these terms" provides: "By creating an account or using Tenure, you agree to these terms on behalf of yourself and the organization you represent... you confirm you have the authority to do so." Read against the ground truth that pilot accounts are provisioned by hand by the vendor and there is no self-registration: a student officer whom the vendor themselves created an account for is deemed to have bound the institution to an unnegotiated instrument, and is then deemed to have accepted every future unilateral amendment by continuing to log in. No student officer has authority to bind the University, and no institution accepts terms that can be rewritten by the counterparty and accepted by silence. These two clauses together are, on their own, a refusal to execute.
  FIX: Strike "Continuing to use the service after a change means you accept the updated terms" and replace with: material changes take effect only on written agreement of the parties, or on 60 days' advance written notice with a right to terminate without penalty and receive a full data return. Strike the "on behalf of the organization you represent" deeming language and replace with: these terms bind an institution only where a signed pilot or services agreement exists, and are subordinate to it.

--- [P0] /story states the pilot as accomplished fact, in the section headed "Early, and honest about it"
  WHERE: src/app/story/page.tsx:187-193
  "Here is exactly where things stand, and where they go this fall. Every organization Simon's Office of Student Engagement stewards puts Tenure to work, and so does the office itself." Simple present tense, stated as current fact, about an office that has verbally agreed and contracted nothing. The vendor's own governance file forbids exactly this (src/lib/site.ts:47-59: "Nothing on the site may state it as settled fact, imply procurement is complete"; src/lib/claims.ts C-021: "MUST be described as planned or proposed... or any implication of completed procurement"). It survived because the enforcement in claims.ts is a list of literal regexes ("is rolling out", "will deploy", "in partnership with") and this is a paraphrase none of them match. From my chair this is a misrepresentation of my institution's procurement position to the public, and if a trademark or communications office reads it next to the University of Rochester lockup two clicks away, it becomes a letter rather than a conversation.
  FIX: "Here is where things stand. We are proposing a Fall 2026 pilot covering the organizations Simon's Office of Student Engagement stewards and the office's own seats. Nothing is contracted." Then add a semantic check to the claims suite — present-tense verbs bound to the office's name — because phrase-list enforcement demonstrably does not catch restatements.

--- [P0] "Immutable audit trail" is false and is contradicted by the vendor's own trust page
  WHERE: src/components/home/TrustStrip.tsx:13
  src/components/home/TrustStrip.tsx:13 presents "Immutable audit trail" as a bare capability chip, above the fold, beside the University of Rochester mark. /trust says the opposite in two places: "Cryptographic tamper-evidence — NOT SUPPORTED... no hash chain, signature or checksum column. Append-only is enforced by the application, not by cryptography or by write-once storage," and "Coverage is 49 of 63 server actions." A row that the application declines to update is not immutable — a migration, a DBA, or anyone with database credentials can alter it — and a trail that omits 14 of 63 server actions is not a complete record either. The vendor's own register (src/lib/claims.ts:278, 350) instructs "never say... cryptographically immutable or WORM," and the forbidden-phrase list catches "tamper-proof" and "hash-chained" but not the unmodified word "Immutable," so the strongest overstatement on the site is the one the guardrail misses. Every other surface (Faq.tsx:34 "create-only", Platform.tsx:262 "Append-only", OfficeConsole.tsx:114 "append-only") gets this right, which makes the outlier read as marketing rather than error.
  FIX: Change the chip to "Append-only audit trail" — accurate, and still a strong claim. Add /\bimmutable\b/i to forbiddenPhrases in src/lib/claims.ts under C-031.

--- [P1] No wind-down, business-continuity or data-return-on-failure clause — for a system of record run by two unincorporated individuals
  WHERE: src/app/terms/page.tsx:117-125; src/app/trust/page.tsx:126-129 ("Bulk export — Roadmap"); no backup/DR control anywhere in src/app/trust/page.tsx
  /terms, Termination: "an organization can ask us to close its account and return or delete its record" and "If we end your access, we'll give your organization a fair chance to retrieve its record where we reasonably can." "Fair chance" and "where we reasonably can" are unenforceable. There is no timeframe, no format, no verification, no certificate of destruction. Worse, /trust states there is no self-service export path — "Export and deletion requests are handled by us, by hand, on request" — so the entire retrievability of an institution's record depends on two individuals answering email. And nothing anywhere addresses the case that matters most: the founders graduate, take jobs, or stop. There is no escrow, no wind-down data-return obligation, no successor obligation, no change-of-control or assignment clause. Separately, and startlingly for a page written for security review, the word "backup" appears on /trust only in the phrase "backup approver" — there is no backup, restore, RPO/RTO or disaster-recovery control listed at all. A product whose entire thesis is "the record survives the people" has no clause and no control ensuring the record survives the vendor.
  FIX: Add to Terms: on termination for any reason, or on cessation of the business, the institution receives a complete machine-readable export within 30 days at no charge, followed by deletion and written confirmation within 60 days. Add a wind-down clause naming a trigger and a named individual obligated to perform it. Add a backup/DR control to /trust with an honest status — even "RDS automated snapshots, retention N days, restore never tested" is worth more than silence.

--- [P1] Terms authorise processing to "improve the service" — which breaks the FERPA school-official posture and contradicts /privacy
  WHERE: src/app/terms/page.tsx:67-71 vs src/app/privacy/page.tsx:60-68
  /terms, Your account and content: "We process it only to provide and improve the service for you, as described in our Privacy notice." /privacy says the opposite: "We use it to deliver the service you're asking for, not to build a business out of your data." Two documents published the same day disagree about the lawful purpose. This is not cosmetic. The FERPA school-official exception (34 CFR 99.31(a)(1)(i)(B)) requires the vendor to be under the institution's direct control as to use and maintenance of education records and to use them only for the authorized purpose. "Improve the service" is a purpose of the vendor's own, not ours, and it is the single clause that would fail our FERPA review even though the FERPA prose itself (see below) is well hedged. The Terms sentence also silently overrides the Privacy sentence via "as described in our Privacy notice," while saying more than the Privacy notice does.
  FIX: Strike "and improve" from the Terms sentence. Add: "Where an institution's records include education records under FERPA, Tenure acts as a school official under the institution's direct control, uses those records only as the institution directs, and will not redisclose them except to the subprocessors named in the Privacy notice."

--- [P1] Subprocessor disclosure is materially thinner on /privacy than on /trust; AWS and Calendly are never named as subprocessors
  WHERE: src/app/privacy/page.tsx:84-100 and 69-82; src/app/trust/page.tsx:111-113, 143-148; src/components/site/Scheduler.tsx
  /privacy names exactly one third party — Anthropic — and disposes of the rest with "Service providers we rely on to operate Tenure, for example, hosting, infrastructure, and the AI provider described above." "For example" is not a subprocessor list. AWS is plainly the hosting and storage provider (the vendor's own /trust page says "Keys are AWS-managed") but is never named on the page a data-protection reviewer actually reads. Calendly, which receives the name, email and meeting content of anyone who books through /contact, is named nowhere in the privacy notice at all. There is no list, no processing locations, no statement that data stays in the US, no notice-of-change right, and no right to object to a new subprocessor. There is also no retention statement of any kind — the word "retention" appears nowhere in the source. Finally, /privacy is silent on the fact /trust discloses as a limit: "There is no per-tenant key, no per-tenant usage quota, and no per-tenant opt-out" for the Anthropic call. Whether an institution can decline the AI subprocessor entirely is a first-page question in every questionnaire I send, and the answer — no — is buried under a control heading on a different page.
  FIX: Replace the "for example" bullet with a named table: Amazon Web Services (hosting, database, object storage — US region X), Anthropic PBC (model inference), Calendly (scheduling, marketing site only). Add processing location, a retention period per data class, a commitment to 30 days' notice before adding a subprocessor with a right to terminate, and move the "no per-tenant opt-out" limit into the privacy notice verbatim.

--- [P1] No security-incident notification obligation anywhere — "breach" appears only as something the customer must not do
  WHERE: src/app/terms/page.tsx:78-83 (only occurrence); src/app/privacy/page.tsx:129-142
  Grep the whole source tree for "breach" and the only hit outside a font licence is /terms, Acceptable use: "Breach the rights of others, including privacy and intellectual property." The vendor's sole use of the word is an obligation imposed on us. There is no undertaking to detect, contain, or notify us of a security incident; no notification window; no content requirements; no obligation to cooperate with our own notification duties under state breach-notification law (NY GBL §899-aa applies to student names plus email credentials, which this product holds). /privacy's Security section says only "we use reasonable safeguards" and "we can't promise absolute security." For a product holding an institution's finance records, member rosters and documents, the absence of any incident-notification clause is disqualifying on its own.
  FIX: Add to both documents: "We will notify the institution's designated contact without undue delay and in any event within 72 hours of becoming aware of a security incident affecting its records, provide the information the institution needs to meet its own notification obligations, and cooperate with its investigation at our expense."

--- [P1] The right to deletion offered in /privacy is one the product is built to refuse
  WHERE: src/app/privacy/page.tsx:144-150 vs src/app/privacy/page.tsx:102-109 and src/app/trust/page.tsx:120-129
  /privacy, Your choices: "You can request access to the information we hold about you, ask for an export, or request deletion." Two sections earlier the same page says "Individuals do not take the record with them when their term ends," and /trust says "History cannot be deleted — A seat carrying assignments, holdings or knowledge is refused deletion and must be retired instead... This is deliberate: it is the property the whole product rests on." So the deletion right is offered on one page and architecturally refused on another, and /privacy never tells the reader which wins. The export right has the same problem: /trust says there is no self-service export and it is done "by hand, on request" — the register at src/lib/claims.ts:534 explicitly permits the privacy page to offer a manual export, but the page never says it is manual or gives a turnaround. No response timeframe, no identity-verification process, no appeal route is stated for any of the three rights.
  FIX: Rewrite as: "Access and export: we will provide a copy within 30 days; this is fulfilled manually today, not self-service. Deletion: personal contact details can be removed, but organizational records and decision history belong to the organization and are retained — a departing officer's access is revoked, the record is not deleted. Where the organization asks us to delete its own record, we will do so within 30 days and confirm in writing."

--- [P1] Liability, indemnity and insurance: one weak carve-out, and every clause I would insist on is missing
  WHERE: src/app/terms/page.tsx:106-115
  /terms, Limitation of liability, is three sentences of tone and one sentence of substance: "To the extent the law allows, Tenure isn't liable for indirect or consequential losses arising from using the service during the pilot," preceded by "We're an early-stage team doing our honest best," which is not contract language. Absent entirely: any IP-infringement indemnity (a standing requirement in every software agreement I sign), any data-breach indemnity, any insurance requirement (cyber, E&O, general liability), any cap or carve-out structure, and any disclaimer/allocation that would survive review. Note also that the limitation is asserted by "Tenure," which the same document says is not a legal person — in practice liability sits with two individuals personally and uninsured, which is itself a vendor-viability finding: a single claim would end this vendor and take our record with it. And the closing sentence — "Keep your own copies of anything truly critical" — is the vendor advising me, in writing, to maintain the shadow drive their entire product exists to abolish.
  FIX: Add a mutual liability framework with a stated cap and standard carve-outs (breach of confidentiality, data-security breach, IP indemnity, gross negligence). Add a vendor IP-infringement indemnity with defence and settlement obligations. Add evidence of cyber/E&O insurance, or state plainly that none is carried so I can price the risk. Delete "Keep your own copies of anything truly critical" or reframe it as a pilot-period statement with a defined export cadence.

--- [P1] No governing law, venue, confidentiality, notices, assignment, publicity, or severability — in a document that claims to form a contract
  WHERE: src/app/terms/page.tsx (entire document, 33-140)
  The document opens "By creating an account or using Tenure, you agree to these terms," so it purports to be a contract. It then omits every boilerplate clause that makes one operable. Searching the source: no "governing law", no "indemn", no "insur", no arbitration, no severability, no notices provision, no assignment or change-of-control clause, no entire-agreement clause, and — the one I would insist on hardest — no confidentiality obligation running from Tenure to the institution. /privacy asserts that service providers are "under confidentiality obligations"; Tenure itself never undertakes one. There is also no publicity restriction, which matters concretely: this vendor already displays the University of Rochester lockup and names the office repeatedly, and nothing in the terms would stop them naming us as a customer the day after signature.
  FIX: Add: governing law and exclusive venue (New York, courts of Monroe County; no arbitration, no jury waiver, no fee-shifting); mutual confidentiality with a 3-year tail; notices to a named address; no assignment without consent, including on change of control; no use of the institution's name, marks or the fact of the relationship in any publicity without prior written consent; severability; entire agreement subordinating these terms to any executed pilot agreement.

--- [P1] /pilot tells 26 student organizations they are enrolled, on the same page that says nothing is contracted
  WHERE: src/app/pilot/page.tsx:209-215 vs src/app/pilot/page.tsx:176-184
  Page header: "Scope and timing are proposed, not contracted." Two sections later: "One term, hands-on the whole way. No waiting list, no application, and no placeholder accounts. If OSE stewards your organization, you're in." That is an unconditional enrolment promise made directly to the office's constituent organizations, on the office's behalf, before the office has agreed to anything. It bypasses the office as the decision-maker and creates an expectation among 26 student boards that my office will then have to unwind if we decline. The self-contradiction inside a single page is also the kind of thing that makes me distrust the careful hedging elsewhere.
  FIX: "If the pilot proceeds, the proposed scope is every organization OSE stewards — no waiting list and no application. That decision is the office's to make."

--- [P1] The site advertises that it loaded the office's real leadership roster, and that it demos a real organization's record to prospects
  WHERE: src/lib/site.ts:125; src/app/contact/page.tsx:16
  Two statements read together create a data-governance problem no clause addresses. src/lib/site.ts:125 promotes "209 seats, built from the office's own leadership roster rather than invented for a demo" — i.e. the vendor obtained and loaded an institutional roster (106 filled seats, 19 advisors per claims.ts C-014 evidence) into a product database with no agreement, no DPA and no FERPA determination in place. And src/app/contact/page.tsx:16 promises prospects: "We open a real organization's record — seats, approvals, the budget, the handoff packet — and you ask what you want to see." With no confidentiality obligation in the Terms (see above), the plain reading is that our record would be opened for the next prospect. The vendor is selling the authenticity of the data precisely where authenticity is the liability.
  FIX: On the metric, say what is defensible: "209 seats modelled from the office's published organization structure." If real names were loaded, be able to say under what basis. On /contact, change to "We open a demonstration organization built on realistic data — seats, approvals, the budget, the handoff packet," and add a confidentiality clause to Terms so the answer to "will you show ours?" is written down.

--- [P1] /terms is silent on fees, payment, taxes and whether the pilot is free
  WHERE: src/app/terms/page.tsx (no fee clause); src/components/home/Faq.tsx:45
  The document governs "how you and your organization may use Tenure while the product is in early access" and covers the pilot by name, but contains no fee, payment, invoicing, tax, renewal or price-change provision, and never says the pilot is provided at no charge. The only commercial statement on the site is the home FAQ: "Pricing is per portfolio, not per club: one budget line for every organization an office stewards" — a pricing model with no number, no unit and, notably, no row in the claims register that governs every other material claim on this site. Whether this is a $0 evaluation or a purchase determines whether I need a data agreement or a competitive-bid waiver, and I cannot tell.
  FIX: Add one sentence to Terms: "The Fall 2026 pilot is provided at no charge. No fees are payable unless and until a separate written agreement is signed." (Or state the fee.) Add a claim row governing the pricing sentence.

--- [P2] The claims register's own "limits must travel with the claim" rule is broken on /privacy
  WHERE: src/app/privacy/page.tsx:129-135 and 69-82 vs src/lib/claims.ts:296-301 and 390-391
  src/lib/claims.ts defines qualification as "Limits that MUST travel with the claim wherever it appears." C-016 lists its locations as ["/trust", "/privacy"] with the qualification "Keys are AWS-managed. There is no customer-managed KMS key and no BYOK. The exports bucket has no explicit encryption block." /privacy's Security section makes the encryption claim and carries none of it. C-007 likewise lists /privacy and carries the qualification "One platform-wide key serves all tenants; there is no per-tenant key, quota or opt-out" — also absent from /privacy. The result is that the legal document, which is the one that would be attached to an agreement and relied on, is systematically less accurate than the marketing trust page. That inversion is exactly backwards, and it means the governance machinery is not actually being applied to the two pages where accuracy is legally operative.
  FIX: Add to the privacy Security paragraph: "Encryption keys are managed by our cloud provider; there is no customer-managed or bring-your-own key today." Add to the AI paragraph: "One platform-wide API key serves all tenants; there is no per-tenant key and no per-tenant opt-out from the assistant." Then extend claims.spec.ts to assert qualification presence on every listed route, not just phrase absence.

--- [P2] Fabricated University of Rochester email addresses published in a marketing mock
  WHERE: src/components/home/Handoff.tsx:35, 44, 53, 62
  src/components/home/Handoff.tsx renders four addresses on the public home page: m.lee@u.rochester.edu (line 35), maya.chen@alum.rochester.edu (44), i.duarte@alum.rochester.edu (53), t.reyes@u.rochester.edu (62). These use a live institutional domain in a first-initial-dot-lastname pattern; at an institution of this size, at least one is likely to resolve to a real person, who will receive scraped spam because a vendor put their address in a public HTML page. It also places invented personal data inside the University's namespace, on the same page as the University's trademark, with no agreement in place. RFC 2606 reserves example.com for precisely this purpose.
  FIX: Change all four to @example.edu. Grep for any other real-domain addresses in mock data before shipping.

--- [P2] The AI mock cites a bylaws section, implying document-content retrieval the product does not have
  WHERE: src/components/home/AiOnboarding.tsx:28
  src/components/home/AiOnboarding.tsx:28 shows an answer footnoted "Bylaws §4 · 2 records." A section-level citation into a bylaws document tells the reader the assistant read inside that document. /trust says the opposite: "Document file contents are not indexed, only titles and descriptions," and retrieval is "keyword matching over five record kinds" with no semantic search. In a security review I would treat a product screenshot as a representation of capability; this one is not accurate, and it sits in the same section as an otherwise commendably frank subprocessor disclosure.
  FIX: Change the source label to something the retrieval layer can actually produce — e.g. "Elections knowledge card · 2 records" — and keep §-level citations off the site until document contents are indexed.

--- [P2] Calendly's consent banner is suppressed, and the privacy notice covers no website visitors at all
  WHERE: src/components/site/Scheduler.tsx:34; src/app/privacy/page.tsx (no website-visitor section)
  src/components/site/Scheduler.tsx:34 loads the Calendly embed with hide_gdpr_banner=1. That parameter exists to suppress a third party's own cookie/consent notice, and the site does not replace it with one of its own: /privacy is written entirely about records inside the product and says nothing about the marketing website — no cookies, no local storage, no third-party embeds, no server logs, no IP addresses, no visitor data of any kind. Booking transmits a name, email and free-text meeting note to a US processor that is named nowhere in the notice. The rest of the site's privacy engineering is genuinely good — no analytics, a tight CSP, Calendly gated behind an explicit click on one route — which makes the deliberate suppression of the one consent surface the odd note in an otherwise careful file.
  FIX: Remove hide_gdpr_banner=1, or add a short "This website" section to /privacy covering cookies/local storage, the Calendly embed, what it collects and links to Calendly's own notice.

--- [P2] No accessibility conformance statement or VPAT for the product
  WHERE: src/app/trust/page.tsx (no accessibility group in GROUPS)
  Nothing on the site addresses accessibility conformance: no VPAT, no Section 508 or WCAG 2.1 AA statement, no accessibility contact, no remediation commitment. Every institutional software procurement I run requires an accessibility review before signature, and a product intended for use by all student officers, including officers with disabilities, cannot skip it. This is a conspicuous gap on a /trust page that is otherwise organised exactly the way I would want — status vocabulary defined up front, limits stated in the vendor's own words, "Not supported" used honestly rather than avoided.
  FIX: Add an "Accessibility" group to /trust with an honest status — even "Roadmap: no VPAT exists; the application has not been audited against WCAG 2.1 AA" is more useful to me than silence, and it is consistent with how the rest of that page behaves.

--- [P2] "Last updated" is a hardcoded string with no effective date and no version history
  WHERE: src/app/privacy/page.tsx:14; src/app/terms/page.tsx:14-19
  Both documents say "Last updated 2 August 2026" as literal prose in the JSX (privacy/page.tsx:14, terms/page.tsx:17). There is no effective date distinct from the update date, no version number, and no archive of prior versions — while the change-notice mechanism in /privacy is "we'll update the date at the top of this page," i.e. notice by me remembering to check. If we ever dispute what was in force in October, neither party can prove it. That is an uncomfortable gap for a vendor whose product is sold on the premise that a record should prove what was decided and when. Being hardcoded, the date will also silently go stale on the next copy edit that does not touch it.
  FIX: Move the date into src/lib/site.ts as a single dated constant used by both pages, add "Effective" alongside "Last updated", add a version number, and keep prior versions at stable URLs so the terms in force on any date can be produced.

--- [P3] "Contact Sales" survives in the footer and FAQ after the site standardised on "Book a walkthrough"
  WHERE: src/components/site/SiteFooter.tsx:45; src/components/home/Faq.tsx:94
  src/lib/site.ts:23-27 states the rule and the reason: "One conversion phrase, used everywhere. 'Contact Sales' oversold a two-founder company and set the wrong expectation for who picks up." The label was then left in two places: SiteFooter.tsx:45 ("Contact Sales") and Faq.tsx:94 ("Contact sales"). Minor, but it works against the same expectation-setting the rest of the site does well — /contact promises "There is no SDR and no qualification call," and then the footer offers me a sales desk.
  FIX: Use site.ctaLabel in both, and add an e2e assertion that the string "contact sales" does not appear in rendered copy.

--- [P3] The claims register cites a component that is never rendered
  WHERE: src/components/site/SupporterStrip.tsx (unused); src/lib/claims.ts:599
  src/lib/claims.ts:599 records C-022 (permission to display the Simon/University of Rochester and Startup Wednesday marks) as appearing at "components/site/SupporterStrip.tsx", "/" and "/story". SupporterStrip is imported by nothing — the marks now render through TrustStrip on the home page only, and /story references them as text rows, not marks. Trivial in itself, but the register's value to me is that it is an accurate inventory of where each claim is exposed; a stale location is a small crack in the one artefact on this site I would otherwise cite approvingly in my review.
  FIX: Delete the dead component and correct C-022's `where` to the surfaces that actually render the marks.

========================================================================================
## Keyboard-only user with low vision — 200% browser zoom, visible focus required, screen reader used for structural navigation (heading list, landmarks), no mouse.
========================================================================================
FIRST TRUST FAILURE: Reading /trust by heading list. I get "Per-organization scoping for institution staff", "Separation of duties on approvals", "Cryptographic tamper-evidence", "Single sign-on (SAML / OIDC)" — and every one of them reads as a shipped capability, because the StatusBadge is a sibling `<span>` outside the `<h3>` (trust/page.tsx:271-274). The page whose entire premise is "we tell you what is not built" structurally reads, to my primary navigation mechanism, as a feature list. That is the exact inversion the page exists to prevent, and it made me distrust the rest of it.
FIRST COMPREHENSION FAILURE: Tabbing off "Explore the platform" in the hero and landing on five buttons announced as "Finance, toggle button, not pressed", "Calendar, toggle button…", "Approvals…" — I spent several seconds believing the site had a product nav. Nothing in the accessibility tree says DashboardMock is a picture of an app I cannot log into. Its sibling decoration (HeroFloatingCards) is correctly `aria-hidden`; this one is not.
FIRST BLOCKED ACTION: Reaching "Book a walkthrough" from the mobile nav panel at 200% zoom — I can Tab to it, but it is rendered off the bottom of a `position: fixed` header that cannot scroll, so I can never see the control I am focused on.
WOULD NOT SURVIVE A DEMO: MetricsBand. "Every number here is counted, not projected." (MetricsBand.tsx:74) sitting directly above four tiles reading 0, 0-gate, 0, 0. `useInView(ref, { once: true, amount: 0.4 })` (MetricsBand.tsx:53) passes 0.4 straight through as an IntersectionObserver threshold (verified in framer-motion/dist/es/render/dom/viewport/index.mjs), and 40% of that section is never simultaneously visible once the viewport is short enough. Demo this on a projector at zoom, or on a 1280x720 laptop at 200%, and the evidence section claims zero end-to-end tests. NumberFlow renders `role="img" aria-label="0"`, so a screen reader is told "0" too.

--- [P0] Mobile nav panel is clipped inside a fixed header with no scroll — the primary CTA is unreachable at 200% zoom
  WHERE: src/components/site/SiteHeader.tsx:88-95 and 164-186
  The panel (SiteHeader.tsx:164-186) is a child of `<header className="fixed inset-x-0 top-0 z-50 …">` (line 88-95). Neither the header nor the panel has `max-height` or `overflow-y: auto`. Measured content height: 68px bar + 8 (pt-2) + 4 links at 52px with gap-1 (220) + theme toggle row (12+32) + CTA row (12+44) + 24 (pb-6) = 420 CSS px. The `lg:` breakpoint is 1024px, so at 200% zoom any screen narrower than 2048 physical px gets this panel: 1366x768 → 683x384 CSS px, 1280x720 → 640x360. The panel overflows by 36-60px and a `position: fixed` box cannot be scrolled, so the browser cannot bring the focused element into view either. `ContactSales size="md"` — the site's single conversion action — is the last item and the first thing lost. This is WCAG 1.4.10 Reflow (AA) and 2.4.11 Focus Not Obscured (AA), and it is worse at the 400% the reflow SC actually requires.
  FIX: Move the panel out of the fixed header, or give it `max-h-[calc(100dvh-68px)] overflow-y-auto overscroll-contain` on the `#mobile-menu` div. The focus trap and Escape handling already work; only the box does not.

--- [P1] MetricsBand renders 0 for every metric when the section is taller than 2.5 viewports
  WHERE: src/components/home/MetricsBand.tsx:53 (and :21, :74)
  `const inView = useInView(ref, { once: true, amount: 0.4 })` (line 53) and `const value = inView || reduce ? metric.value : 0` (line 21). `amount` is handed to IntersectionObserver as `threshold: 0.4` verbatim; `entry.isIntersecting` only fires once the ratio crosses it, and the ratio for a target taller than the root caps at rootHeight/targetHeight. At 400% zoom (320 CSS px wide → `grid-cols-1`, four stacked tiles) the section is ~1450 CSS px against a ~200 CSS px viewport: max ratio 0.14, so it never fires and the numbers stay 0 permanently. At 200% on a 1280x720 laptop the margin is already gone (~0.38). The rendered result is "0 organizations modelled", "0-gate approval chain, 7 request types", "0 end-to-end tests" under the sentence "Every number here is counted, not projected." Two of those are false as rendered, and NumberFlow publishes them to AT as `aria-label="0"`.
  FIX: Use `amount: "some"` (threshold 0), or drop useInView entirely and render `metric.value` as the initial NumberFlow value — the count-up is decoration, the number is content and must never depend on geometry.

--- [P1] Two auto-rotating regions can only be paused with a mouse — WCAG 2.2.2, Level A
  WHERE: src/components/visuals/DashboardMock.tsx:355-367; src/components/home/SeatMechanism.tsx:111-115, 162-163
  DashboardMock advances modules every 4200ms (DashboardMock.tsx:355-361) and SeatMechanism advances terms every 3400ms (SeatMechanism.tsx:111-115). Both gate pausing on `onMouseEnter`/`onMouseLeave` only (DashboardMock.tsx:366-367, SeatMechanism.tsx:162-163). Reduced motion stops them, but reduced motion is not the same preference as "I read slowly" — I run 200% zoom with motion on. Focusing the region does nothing; the only keyboard route to `setPaused(true)` is *activating* a dot or module button, which also changes what I was reading. Meanwhile the content swaps under me every 3.4s: SeatMechanism replaces three occupant rows, a record count and a fresh-record line via AnimatePresence while I am partway through them. Both regions carry real prose, not decoration.
  FIX: Add `onFocus`/`onBlur` (or `focusin`/`focusout` on the container) alongside the mouse handlers, and add a visible pause/play control. Simplest honest fix: stop auto-advancing and let the dots be the only driver.

--- [P1] Secondary buttons have no perceivable boundary in either theme; `--border-strong` exists for exactly this and is never used on a control
  WHERE: src/components/ui/Button.tsx:19-21; src/app/globals.css:55-59; src/components/site/ThemeToggle.tsx:104
  `variants.secondary` is `bg-cloud text-ink border border-line` plus `shadow-sm` (Button.tsx:19-21). Computed contrast: light theme, white fill vs the paper canvas = 1.07:1, `border-line` vs canvas = 1.24:1, `shadow-sm` is `oklch(24% … / 0.06)` — imperceptible. Dark theme is no better: surface vs canvas 1.08:1, border vs canvas 1.39:1. WCAG 1.4.11 wants 3:1 for the visual information identifying a control. So "Explore the platform" in the hero, "See the product" on the 404, "See the pilot" on /product, and — worst — "Or pick a time here" on /contact (the only control that opens the inline calendar, Scheduler.tsx:74) are text floating on the page with an invisible box around them. globals.css:55-56 literally documents the intent — "`border-strong` is load-bearing (it can be the only thing marking a control boundary) and so is held to the 3:1 non-text requirement" — and `--border-strong` (4.53:1 vs canvas) appears exactly once in the whole codebase, as a decorative left rule on /trust:280. The ThemeToggle shell has the same problem (`border-line bg-surface`, ThemeToggle.tsx:104), which leaves it reading as three loose 14px icons.
  FIX: Change `secondary` and the ThemeToggle shell from `border-line` to `border-border-strong`. The token is already defined, already passes, and already has a CI contrast checker pointed at it.

--- [P2] Two headings on the home page have byte-identical text at different levels
  WHERE: src/components/home/Handoff.tsx:286 and src/components/home/AiOnboarding.tsx:76
  Handoff.tsx:286-289 renders `<h3>` "They read the seat before they sit in it." and AiOnboarding.tsx:76-79 renders `<h2>` "They read the seat before they sit in it." — same sentence, only the span split differs. They are adjacent sections (6 and 7 in page.tsx's own ordering). Navigating the home page by heading, which is how I find anything, produces two indistinguishable entries and no way to know which section I am jumping to. page.tsx:25 claims "The sequence below is the argument, once each" and page.tsx:23 says four arguments were previously made twice — one of them still is, at the level a screen reader is most sensitive to.
  FIX: Retitle one. Handoff's section is about the packet, so make its h3 "Shadow access starts before day one" or similar; leave AiOnboarding's h2 as the memorable line.

--- [P2] /trust status badges sit outside the heading, so heading navigation reports every unsupported control as shipped
  WHERE: src/app/trust/page.tsx:270-275; src/components/ui/StatusBadge.tsx:49-59
  `<h3>{c.title}</h3>` and `<StatusBadge status={c.status} />` are flex siblings (trust/page.tsx:270-275). A heading-list jump gives me only the title. "Separation of duties on approvals" / "Per-organization scoping for institution staff" / "Cryptographic tamper-evidence" / "Single sign-on (SAML / OIDC)" all read as capabilities Tenure has. The badge itself is also bare text with no role or prefix, so even reading linearly I get "…approvals. NOT SUPPORTED." as a loose fragment with nothing tying it to the heading. StatusBadge is otherwise well built — the word is always rendered, colour is never the sole carrier, and "Live" vs "Verified in CI" share a colour but differ in text, which is right.
  FIX: Put the status inside the heading, or add a visually-hidden prefix inside StatusBadge (`<span className="sr-only">Status: </span>`) plus `aria-describedby` from the h3 to the badge. Cheapest correct version: move `<StatusBadge>` inside the `<h3>`.

--- [P2] DashboardMock puts five fake product buttons in the tab order with no indication they are an illustration
  WHERE: src/components/visuals/DashboardMock.tsx:403-416
  DashboardMock.tsx:403-416 renders real `<button aria-pressed>` elements labelled Finance / Calendar / Approvals / Members / Memory, inside the hero (Hero.tsx:86) and again on /product:144. They are keyboard-reachable and announce as toggle buttons. Nothing marks the region as a mock — no `role="img"` with a label, no caption, no `aria-hidden`. HeroFloatingCards, which is genuinely decorative, gets `aria-hidden` (HeroFloatingCards.tsx:16) and HeroShapes gets it too (HeroShapes.tsx:37), so the omission reads as an oversight rather than a decision. On the home page these five sit between the hero CTAs and the TrustStrip, so the first thing after the conversion buttons is a fake app.
  FIX: Wrap the mock in `<figure>` with a `<figcaption className="sr-only">Illustration: the Tenure workspace</figcaption>`, and either mark the whole thing `aria-hidden` (the tour is decorative) or at minimum give the module buttons an accessible group name like "Illustration modules".

--- [P2] The handoff seat table is a div grid, and its per-cell labels are display:none at exactly the widths where the column heads exist
  WHERE: src/components/home/Handoff.tsx:205-261 (labels at :146-152)
  Handoff.tsx:205-261 is genuine tabular data — five columns, four rows — built as a `<ul>` of grid `<li>`s. The column heads (lines 205-211) are `hidden … md:grid`; the per-cell `ColumnLabel`s (line 148) are `md:hidden`. So at md and above a screen reader gets five orphan labels followed by four rows of unlabelled values, with no header-to-cell association at all, and the first column never has a ColumnLabel in any case. Reading row 4 I get "VP Sponsorship, SCC-VP-SPON, Vacant, Tomas Reyes '26, t.reyes@u.rochester.edu, 27, Shadow, Jordan Kim" and have to hold the header order in my head. This is the section the page calls "the proof".
  FIX: Use a real `<table>` with `<th scope="col">` and keep the responsive stacking with CSS grid on the table element (`display: grid` at small widths), or keep the divs and add `role="table"/"row"/"columnheader"/"cell"`. The former is less code.

--- [P2] Scheduler disables its own button mid-load, dropping focus to <body> with no announcement
  WHERE: src/components/site/Scheduler.tsx:70-77, 93-108
  `disabled={state === "loading"}` (Scheduler.tsx:73) on the button I just activated. The browser blurs a disabled element, so focus lands on `<body>`; when `loadCalendly()` resolves the button re-enables but focus is gone and I have to Tab from the top of the document. Nothing announces the transition either: the "Loading calendar…" label change happens on a disabled control, and the 680px embed at line 110-114 appears silently. On failure, `role="status"` (line 95) is on a paragraph that is *mounted at the same moment as its content*, which many screen readers do not announce because the live region did not exist before the change. Everything else about this component is right — the plain-anchor fallback, the sr-only "(opens in a new tab)", the real `aria-expanded`/`aria-controls` disclosure.
  FIX: Replace `disabled` with `aria-busy` + `aria-disabled` and an early return in the handler so focus is retained. Render an always-present `<p role="status" className="sr-only">` container and write the loading/ready/failed message into it.

--- [P2] The hero's #platform link scrolls the page without moving focus
  WHERE: src/components/home/Hero.tsx:69 → src/components/home/Platform.tsx:85
  `<Button href="#platform" variant="secondary" size="lg">Explore the platform</Button>` (Hero.tsx:69-71) targets `<section id="platform" … scroll-mt-20>` (Platform.tsx:85). The `scroll-mt-20` correctly clears the 68px fixed header — that part is right. But the section is not focusable (`tabIndex` absent), so activating the link moves the viewport 8 screens down while my focus stays on the button in the header area. My next Tab takes me back to the top of the document, which at 200% zoom means I have no idea where I am. Same pattern as the skip link, which does this correctly (`<main id="main" tabIndex={-1}>`, layout.tsx:77) — the fix is already in the codebase, just not applied here.
  FIX: Add `tabIndex={-1}` to `<section id="platform">`, matching what `<main id="main">` already does.

--- [P3] Supporter logos use mix-blend-multiply and vanish in dark theme
  WHERE: src/components/home/TrustStrip.tsx:33
  TrustStrip.tsx:33 applies `opacity-90 mix-blend-multiply` with no dark-mode variant, on a `bg-cloud` parent (line 18) which is `oklch(21.5% …)` in dark. site.ts:100 notes the Simon lockup is the "navy RGB" variant. Multiplying navy artwork into a near-black surface produces near-black — the two marks the site has written permission to display disappear entirely. I use dark theme at 200% zoom, so the whole strip reads as an empty band between the hero and the Problem section. The `alt` text is still exposed to AT, which makes it worse: my screen reader tells me there are two logos I cannot see.
  FIX: `mix-blend-multiply dark:mix-blend-normal` won't work here since the theme is token-driven, not `dark:`-driven — scope it with `:root[data-theme="dark"] .supporter-mark, @media (prefers-color-scheme: dark) :root:not([data-theme="light"]) .supporter-mark { mix-blend-mode: normal }`, or ship a light-on-dark asset variant.

--- [P3] Sub-11px monospace type is used in ~40 places, overriding the stated 11px floor
  WHERE: src/components/visuals/DashboardMock.tsx:234; src/components/home/SeatMechanism.tsx:253; src/components/home/Handoff.tsx:206-210, 250; src/components/home/Platform.tsx:21
  globals.css:427-428 defines `label-mono` at 0.6875rem (11px) with the comment "at 11px these are the smallest type on the page and need the extra contrast". Components then override it downward 40 times: `text-[0.5rem]` (8px, SeatMechanism.tsx:253), `text-[0.52rem]` (DashboardMock.tsx:234, :318), `text-[0.54rem]` (Platform.tsx:21, Handoff.tsx:250, OfficeConsole.tsx:360, DashboardMock.tsx:34/117/126/214/418), `text-[0.55rem]`/`[0.56rem]` across Handoff, OfficeConsole, Platform, ProductAtWork. These are uppercase mono with `tracking-[0.14em]` — the worst legibility combination — and they carry real content (column headers, status words like SHADOW/ALUMNI/Vacant, the audit-row example). Contrast is fine everywhere I measured (5.2-7.4:1); the problem is purely size. At 200% zoom 8px becomes 16px, which is only barely where body text should have started.
  FIX: Set a floor: nothing below `label-mono`'s own 0.6875rem. If the layout does not fit at 11px, the layout is the problem — these are the strings that describe what the mock is showing.

--- [P3] aria-controls on the hamburger points at an element that does not exist when the menu is closed
  WHERE: src/components/site/SiteHeader.tsx:136 vs 164
  `aria-controls="mobile-menu"` is always set (SiteHeader.tsx:136), but `<div id="mobile-menu">` is conditionally rendered and absent whenever `open` is false (line 164). A dangling IDREF is invalid ARIA; JAWS in particular resolves aria-controls eagerly and gets nothing. The comment at lines 162-163 explains why the panel is unmounted (so its links are never Tab-reachable while collapsed) and that reasoning is sound — the attribute just needs to follow it.
  FIX: Drop `aria-controls` (it is optional; `aria-expanded` carries the state), or keep the panel mounted with `hidden` and let `hidden` remove it from the tab order.

--- [P3] `border-radius: 2px` inside the :focus-visible rule restyles the element, not the outline
  WHERE: src/app/globals.css:397
  globals.css:394-398 sets `:focus-visible { outline: 2px solid …; outline-offset: 3px; border-radius: 2px; }`. `border-radius` there changes the *element's* corner radius on focus, not the outline's — outlines follow the element's radius automatically in every current browser. Most focusable elements here carry a Tailwind `rounded-*` utility, which lives in the utilities layer and beats `@layer base`, so the visible damage is limited. But any focusable element that relies on a component-layer radius (or none) will square off the instant I focus it, which reads as a rendering glitch when I am at 200% and watching the focus ring closely. Worth noting the surrounding work is good: the light/dark ring split (`--focus-ring` 7.37:1 on canvas, `--focus-ring-inverse` 9.44:1 on the navy bands) is measured and correct, and the `.on-inverse` / `[class*="bg-band"]` override at :402-406 genuinely works.
  FIX: Delete the `border-radius: 2px` line. Nothing depends on it.

--- [P3] The FAQ is an exclusive accordion, so opening one answer silently closes the one I was reading
  WHERE: src/components/home/Faq.tsx:71
  `<details className="group" name="faq" open={i === 2}>` (Faq.tsx:71). The `name` attribute makes these mutually exclusive: opening "Who owns the record?" collapses whatever was open, with no announcement. If I have navigated into an answer and then open another to compare, content I was reading is removed from the DOM behind me. There is no reason for exclusivity in a four-item FAQ where all four answers are two lines. Separately, `open={i === 2}` starts the *third* item expanded, which is an odd default and means the visual state and the source order disagree for a sighted-plus-AT user cross-checking.
  FIX: Remove `name="faq"` and let items open independently; drop `open={i === 2}` or move it to `i === 0`.

--- [P3] The Calendly embed is hard-coded to a white background, which is a full-brightness flash in dark theme
  WHERE: src/components/site/Scheduler.tsx:34
  `background_color=ffffff` in the widget URL (Scheduler.tsx:34), with the host div at `min-h-[680px]` (line 113). A user on the dark theme who expands the calendar gets a 680px pure-white panel appear beneath a `oklch(17.5%)` page. For low-vision users the dark theme is usually a photophobia or glare accommodation, not a preference, and this defeats it in one click. The rest of the third-party handling is genuinely well done — nothing from Calendly loads until asked (site.ts:15-19, ContactSales.tsx:6-19), the CSP widening is scoped to /contact only (next.config.ts:41-56), and the plain-anchor fallback exists for blocked networks.
  FIX: Read the resolved theme (`document.documentElement.getAttribute('data-theme')`, already the single source of truth per ThemeToggle.tsx:64-70) and pass Calendly's dark `background_color`/`text_color` params when dark is active.

========================================================================================
## Investor doing a first-pass read / engineer deciding whether to join — trying to answer one question: is this a real product or a landing page in front of a demo?
========================================================================================
FIRST TRUST FAILURE: /story, under the heading \"Early, and honest about it\": \"Every organization Simon's Office of Student Engagement stewards puts Tenure to work, and so does the office itself.\" Present tense, about a named institution, for a pilot the site's own /pilot header calls proposed and not contracted — and for a product that has never been run by a real user.
FIRST COMPREHENSION FAILURE: What it costs and who buys it. The one pricing sentence on the site (\"per portfolio, not per club\") is a model with no number, no pilot terms, and no statement of whether the office or the club holds the budget — so I cannot size a deal or tell whether this needs a purchase order.
FIRST BLOCKED ACTION: See the product. There is no screenshot, no recording, no sandbox, no app URL, no repo link — every \"product surface\" on the site is a bespoke React illustration with invented people, and the only path to seeing Tenure is booking a 30-minute call with a founder.
WOULD NOT SURVIVE A DEMO: The Tenure AI exchanges. The home page shows the assistant answering \"what did we overpay last year?\" with a $1,240 budget variance and answering \"how do we run elections?\" from \"Bylaws §4\" — while /trust states that finance figures are not in the corpus and that document file contents are not indexed. Ask either scripted question in a live walkthrough and nothing comes back.

--- [P0] The flagship AI demos show answers the retrieval layer cannot produce — /trust says so on the same site
  WHERE: src/components/home/SeatMechanism.tsx:269-275; src/components/home/Platform.tsx:118-127; src/components/home/AiOnboarding.tsx:26-29 vs src/app/trust/page.tsx:158-162
  Three separate mockups depict Tenure AI citing sources that are explicitly out of corpus. SeatMechanism.tsx:269-275 shows "Who's our caterer, and what did we overpay last year?" answered with "The '25 gala ran $1,240 over" — a finance figure. Platform.tsx:118-127 and AiOnboarding.tsx:26-29 both show "How do we run elections?" answered from "Bylaws §4 · 2 records" — a document body and a policy. /trust says the opposite in plain words: "Retrieval is keyword matching over five record kinds... There is no semantic or vector search. Document file contents are not indexed, only titles and descriptions. Finance figures and people records are not in the corpus, so those questions cannot be answered by the assistant today" (trust/page.tsx:158-162), and claims.ts:235 forbids implying the AI can answer policy questions. So the two showcase questions on the home page are, per the company's own register, the two categories the assistant cannot answer. This is the single thing that breaks in a live walkthrough: a prospect asks the caterer question and gets nothing back.
  FIX: Replace both scripted exchanges with questions the keyword index over knowledge cards, approvals, events and org records can actually serve — e.g. the sponsorship-pipeline exchange already in AiOnboarding, or "what did the board decide about the fall mixer?" citing a decision card and an approval. Drop the dollar-variance sentence and the "Bylaws §4" citation entirely, and add a claims.spec assertion that no rendered AI mock cites a document section or a currency amount.

--- [P0] /story states the uncontracted pilot as a present fact, under a heading that boasts about honesty
  WHERE: src/app/story/page.tsx:187-192
  "Here is exactly where things stand, and where they go this fall. Every organization Simon's Office of Student Engagement stewards puts Tenure to work, and so does the office itself." Present tense, declarative, naming a real third party — under the heading "Early, and honest about it." Nothing on Tenure is in use by anyone; the pilot is verbally agreed and not contracted; site.ts:48-58 forbids stating it as settled fact and the phrase evades forbiddenPilotPhrases only because the regex looks for "is rolling out / deploying / standing up". If the OSE director reads this page, or a prospective investor calls the office to reference-check, the sentence is false today and commits a named institution to something it has not signed.
  FIX: Rewrite to the same tense the /pilot header uses: "This fall we are proposing to put every organization the office stewards on Tenure, and the office itself. Scope and timing are proposed, not contracted." Then extend forbiddenPilotPhrases with a present-tense pattern (/Office of Student Engagement[^.]{0,60}\b(puts|runs|uses|is on)\b/i) so the regex catches the paraphrase as well as the original.

--- [P1] /pilot tells third-party organizations they are already enrolled in a pilot nobody has signed
  WHERE: src/app/pilot/page.tsx:213-215 and 158-162
  "No waiting list, no application, and no placeholder accounts. If OSE stewards your organization, you're in." That asserts an entitlement on behalf of an office that has made only a verbal commitment, three paragraphs after the page header says "Scope and timing are proposed, not contracted." The same page then ends step 04 with "productive from the very first meeting" — the exact class of unmeasured outcome claim the team deleted from the home page (see the comment at Problem.tsx:15-17: "an outcome nothing measures"). A club president who reads "you're in" and emails OSE creates a problem for the founders, not for OSE.
  FIX: Change to "If OSE stewards your organization and the pilot goes ahead, you are in scope — there is no application to fill in." Replace "productive from the very first meeting" with the mechanism: "the incoming team opens the seat's record and its handoff packet on day one instead of an empty folder."

--- [P1] "Immutable audit trail" on the home page is the exact word /trust warns buyers to interrogate
  WHERE: src/components/home/TrustStrip.tsx:13; contradicted by src/app/trust/page.tsx:96-101
  The home trust strip advertises "Immutable audit trail" as one of four proof chips, directly beside the university logos. /trust says: "The audit table has no hash chain, signature or checksum column. Append-only is enforced by the application, not by cryptography or by write-once storage," and adds "Any vendor claiming immutability should be asked which of those three it means" (trust/page.tsx:96-101). Coverage is also 49 of 63 server actions, not all of them. The forbidden-phrase ratchet catches "tamper-proof" and "hash-chained" but has no rule for bare "immutable", so the strongest and least defensible wording on the site is the one word CI does not check. A security reviewer who reads the home page then the trust page concludes the marketing overstates and the trust page cleans up after it.
  FIX: Change the chip to "Append-only audit trail" (the Platform card at Platform.tsx:262 already uses exactly that). Add { phrase: /\bimmutab(le|ility)\b/i, claimId: "C-031" } to forbiddenPhrases so it cannot come back.

--- [P1] /product ships the unmeasured onboarding claim the rest of the site deleted, plus a false statement of AI corpus scope
  WHERE: src/app/product/page.tsx:213-216 and 80-83
  Section 03 is headlined "Productive in days, not a semester" — MetricsBand.tsx:39-42 records that this comparison was removed from the home page because "nothing in either product repository measures onboarding duration," and Problem.tsx:15-17 records the same deletion. It survives on /product, where a buyer is most likely to quote it back. The supporting bullet is worse: "Tenure AI answers from the seat's full record from day one" (product/page.tsx:81-83). The record is not full — finance, people, policies and document bodies are all outside the corpus. Two of the site's four AI statements are therefore stronger than /trust allows.
  FIX: Retitle the section to the mechanism, e.g. "They read the seat before they hold it" (or "Day one starts with the record, not a folder"), and change the bullet to "Tenure AI answers from the knowledge cards, approvals, events and org records the seat can see — and links every source."

--- [P1] The home logo strip captions the University of Rochester and Startup Wednesday marks as "Planned Fall 2026 pilot"
  WHERE: src/components/home/TrustStrip.tsx:21-38
  TrustStrip renders "Planned {season} pilot" as a label in the same flex row as the Simon Business School / U-of-R lockup and the Startup Wednesday mark. Written permission covers displaying both marks, but permission to display is not permission to imply pilot participation — and Startup Wednesday has nothing to do with the pilot at all, so the caption is wrong for one of the two logos on its face. Read at a glance, this is a customer-logo wall for a deal that is not signed. /story gets this right by separating "Built with" from "Supported by" (story/page.tsx:26-27); the home page collapses them. C-022 explicitly forbids captioning that implies customership or sponsorship.
  FIX: Split the row: label the Simon mark "Built with Simon's Office of Student Engagement" and the Startup Wednesday mark "Supported by", and move the "Planned Fall 2026 pilot" phrase into the chip list where it belongs to the scope chip, not to the marks.

--- [P1] Privacy and Terms promise deletion; /trust says deletion is refused by design
  WHERE: src/app/privacy/page.tsx:144-150 and src/app/terms/page.tsx:117-125 vs src/app/trust/page.tsx:121-124
  Privacy: "You can request access to the information we hold about you, ask for an export, or request deletion... we'll work with you and your organization to honor the request." Terms: "an organization can ask us to close its account and return or delete its record." /trust: "A seat carrying assignments, holdings or knowledge is refused deletion and must be retired instead... This is deliberate: it is the property the whole product rests on." A university counsel or privacy officer reading all three in one sitting sees an unresolved conflict between a data-subject right and the product's core invariant — and this is the one page where being wrong is a legal exposure rather than a marketing one. Neither legal page states a retention period, a deletion SLA, or which records survive an individual's request.
  FIX: State the actual position once, in the privacy notice, and link it from /trust: individual account access is revoked and the person moves to alumni; organizational records created in a seat are retained as the organization's property and are not deleted on an individual's request; institution-level deletion is handled by hand on request. Add a retention line and a response-time commitment.

--- [P1] Nothing on the site is checkable — no screenshot, no video, no demo, no app link, and the one artifact that would prove it is unpublished
  WHERE: src/components/home/ProductAtWork.tsx:199-201; whole site (no /public screenshot assets, no app URL in src)
  Every product surface on the site is a hand-built React illustration with invented people (Marcus Lee, Priya Nair, Prestige Catering). There is not one screenshot of the running application, no recorded walkthrough, no sandbox, no link to the app, no changelog, no engineering blog. ProductAtWork is headlined "Not a pitch deck. The system, actually running" over exactly that — a pitch-deck illustration. The only route to seeing Tenure is a 30-minute Calendly with a founder. Meanwhile the strongest credibility asset the team owns — src/lib/claims.ts, 32 claims with file:line evidence at a named commit and a CI ratchet that fails the build on an unsupported phrase — exists in the repo and is linked from nowhere on the public site. For "is this real or a landing page in front of a demo", the site currently reads as the latter, and the artifact that answers the question is the one thing withheld.
  FIX: Ship two things: (a) three real screenshots or a 90-second screen recording of the seeded environment on /product, captioned as seed data; (b) a public /claims route rendered from claims.ts showing claim, status, and evidence reference (paths without line numbers if the repo is private). Then link it from /trust and from the AI disclosure. Retitle ProductAtWork unless it carries a real screenshot.

--- [P1] The founders are effectively anonymous on a site asking institutions to trust two people
  WHERE: src/app/story/page.tsx:118-138 and 10-28; src/lib/site.ts:31-34
  /story renders both founders with an initial in a square, the role "Co-founder", and the identical body line "Co-founder. Building Tenure at Simon Business School." — generated from site.ts:31-34, where both entries carry the same role string. No photographs, no prior work, no division of responsibility, no LinkedIn links (the footer links a company page only), no founding date. Yet claims.ts assigns clearly different ownership — Satvik owns every engineering and security claim, Almamy owns customer, legal and metric claims — so the split exists and is deliberately not shown. For the two readers this page exists for (an investor doing diligence, an engineer deciding whether to join), the first question is "who are these people and what have they built before," and the page answers neither. "Record of founding" then lists "Founded: Simon Business School, University of Rochester" — a place under a label everyone reads as a date, and no date appears anywhere on the site.
  FIX: Give each founder a real role (e.g. Engineering / Product & Institutional), 2-3 sentences of prior context including whether either held a student-org seat, a photo, and a personal LinkedIn link. Change the "Founded" row to a date and add a separate "Origin" row for the school.

--- [P1] /trust names every unbuilt control but is silent on how anyone actually signs in — and the register's own evidence says dev login is enabled in production
  WHERE: src/app/trust/page.tsx:174-180; src/lib/claims.ts:363-369
  The SSO entry says only "Institutional SSO is not deployed. Pilot access is closed: accounts are provisioned by us in advance." The evidence row behind it cites infrastructure/terraform/ecs.tf:167-168 — AUTH_DEV_LOGIN=true and ALLOW_DEV_LOGIN_IN_PRODUCTION=true — and the register instructs "Do not publish the specifics of the pilot sign-in mechanism." There is no statement anywhere about passwords, MFA, session handling or account recovery. On a page whose entire value proposition is that it enumerates what is missing, a deliberate omission at the authentication layer is the one gap that retroactively discredits the other forty entries if a reviewer finds it later — and a reviewer will, because it is the first question in any security questionnaire.
  FIX: Fix the product first (remove the dev-login flags from the production task definition), then add a "How people sign in" control to the Identity group stating the real mechanism, MFA status, and session expiry. If it cannot be disclosed today, say "not published pending a change we are making before the pilot" rather than leaving the section silent.

--- [P2] The metrics band promises outcome measurement and delivers seed-data and test counts
  WHERE: src/components/home/MetricsBand.tsx:68-75; src/lib/site.ts:120-149
  Heading: "Continuity, measured. We don't measure engagement. We measure whether the knowledge survived." The four tiles are: 26 organizations modelled (seeded from a public roster), a 2-gate approval chain (an architecture fact), 0 records deleted at offboarding (a code invariant, not an observation), and 132 e2e tests. None of them measures whether knowledge survived; nothing can, because no term has been run. The top-line "26" in 3.6rem type invites exactly the traction misread that claims.ts:554-555 forbids ("Never present them as customers, users or adoption"), and the sub-line "built from the office's own leadership roster rather than invented for a demo" is doing heavy work to describe what is, structurally, demo data.
  FIX: Retitle to what the tiles actually are — "What we can show you today, counted from the repository" — and add "seeded" or "modelled, not yet in use" to the 26 tile's own label rather than only its sub-line.

--- [P2] No price, no unit, no budget range — anywhere
  WHERE: src/components/home/Faq.tsx:44-46; no pricing route in src/lib/routes.ts
  The only pricing statement on the site is an FAQ answer: "Pricing is per portfolio, not per club: one budget line for every organization an office stewards." That is a pricing model, not a price. There is no /pricing route, no range, no indication of whether the Fall 2026 pilot is free or paid, and no statement of who holds the budget (the office or the club). An investor cannot compute an ACV; a director cannot tell whether this needs a purchase order or a procurement cycle; and the pricing-model sentence itself has no row in the claims register even though it is a material commercial claim.
  FIX: State the pilot's commercial terms on /pilot in one line (free for the pilot term, or a named figure), give an indicative institutional range or "pricing starts at $X per office per year", and add a register row for the per-portfolio model.

--- [P2] /pilot never says how the pilot is judged, although its own meta description promises exactly that
  WHERE: src/app/pilot/page.tsx (whole route) vs src/lib/routes.ts:48-55
  routes.ts:50-51 markets the page as covering "scope, who does what, onboarding inputs, support model and how success is measured." The page has four sections — what's included, who it's for, four steps, and an institutions CTA — and no success criteria, no dates beyond "Fall 2026", no definition of what the founders and the office each owe each other, and no exit criterion. The question every investor and every director asks in the first ten minutes ("what has to be true in December for this to have worked?") is unanswerable from the page built to answer it.
  FIX: Add a fifth section: 3-5 falsifiable success criteria you would accept being judged on (e.g. every stewarded org has a populated seat map by week 4; the spring handoff packet is generated without an officer writing a document; the office runs approvals in Tenure rather than email for the term), plus a start date, a review date, and what the office is asked to supply.

--- [P2] /trust omits the operational half of a security review: retention, backups, incident response, subprocessors and founder access
  WHERE: src/app/trust/page.tsx:104-131 and 199-222; src/app/privacy/page.tsx:95-99
  The page is unusually strong on architecture and unusually silent on operations. Nothing about backups, recovery objectives, availability, incident response or breach notification. The only named subprocessor is Anthropic — AWS (the actual host, per the encryption evidence) and Calendly are never named on the public site, and privacy/page.tsx:95-99 refers to them only as "service providers we rely on... for example, hosting, infrastructure." There is no data-residency statement and no vulnerability-management line. Most conspicuously, on a page that volunteers that any institution account can read every organization, there is no statement about what the two founders can see in the production database — which is the thing an administrator will actually worry about.
  FIX: Add a "Operations and access" group with four entries: backups and recovery (status as it truly is), incident response and notification commitment, a named subprocessor list (AWS region, Anthropic, Calendly), and a plain statement of founder/administrator access to production data with whatever control does or does not exist.

--- [P2] The market is widened to SMEs and nonprofits on an unevidenced "already configures" claim
  WHERE: src/lib/site.ts:170-179; rendered at src/components/home/WhoFor.tsx:87-89
  Two of the four audience cards sell markets outside the university: "The same seat model applies outside a university" (SMEs) and, more strongly, "The same engine already configures a structurally different organization, programs, committees, sites, and funder relations in place of clubs and boards" (nonprofits). "Already configures" asserts shipped multi-vertical capability. There is no claim row for it in claims.ts, no evidence reference, and no mention of it on /trust or /product. This is the only mechanism by which the site makes the market look larger than one student-affairs office, and it is the least supported sentence on the site — which is precisely where a competitor or a diligence call will push ("show me the non-university tenant").
  FIX: Either add a register row with the evidence that org-type configuration exists in the deploying repo, or soften to the honest form: "The same seat model is designed to carry over to boards and chapters; today it is built and tested against university organizations." Also add one number somewhere on the site (US student orgs per campus, or offices per institution) so the market has a size at all.

--- [P2] Two adjacent home sections carry the identical headline
  WHERE: src/components/home/Handoff.tsx:286-289 and src/components/home/AiOnboarding.tsx:76-79
  Handoff's shadow-access block is headed "They read the seat before they sit in it." and the very next section, AiOnboarding, uses the same sentence as its h2. The home page comment at page.tsx:17-42 states the whole point of the refinement pass was that "four arguments were made twice"; this is the one duplication the pass left, and it is verbatim, in adjacent sections, one of them an h2 and one an h3 — which also reads badly in a heading outline.
  FIX: Keep the line where it earns its place (Handoff, where shadow access is the subject) and give AiOnboarding a headline about the asking — e.g. "Ask the seat what it already knows."

--- [P2] The status vocabulary claims a validation activity that is not happening
  WHERE: src/components/ui/StatusBadge.tsx:22-26; applied at src/app/trust/page.tsx:158
  STATUSES.validating renders as "In pilot validation" with the hint "Built, but not yet exercised at scale with real users," and is applied to Tenure AI retrieval quality on /trust. No pilot is running — it is planned, unsigned, and dated to a term that has not started. The badge asserts an in-flight validation process, on the one page whose entire worth is that its vocabulary is exact.
  FIX: Rename the label to "Not yet validated" or "Built, unproven" and change the hint to "Built, but never exercised with real users. Validation is planned for the pilot."

--- [P2] /contact promises a walkthrough of "a real organization's record"
  WHERE: src/app/contact/page.tsx:15-17
  "We open a real organization's record — seats, approvals, the budget, the handoff packet — and you ask what you want to see." There is no real organization on Tenure. What exists is a model seeded from a public leadership roster, which claims.ts:554-555 requires never be presented as customers or adoption. The word "real" is doing work it cannot support, on the page where the buyer forms an expectation about what they are about to be shown.
  FIX: "We open a full working environment modelled on a real office's roster — 26 organizations, 209 seats — and you ask what you want to see." It is a stronger sentence and it is true.

--- [P2] The partner logos are rendered with mix-blend-multiply and will disappear in dark mode
  WHERE: src/components/home/TrustStrip.tsx:33 (and the dead SupporterStrip.tsx:20); tokens at src/app/globals.css:112
  Both logo images use `mix-blend-multiply` over `bg-cloud`. In light mode --surface is white and the blend correctly knocks out the PNG background. In dark mode --surface is oklch(21.5% 0.021 250), so multiply drives the navy University of Rochester lockup toward black on near-black — the mark either vanishes or reads as a smudge. The site now has a real, system-following dark theme, and roughly half of first-time visitors on modern OS defaults will land in it, so the only third-party proof point on the home page is invisible for them.
  FIX: Drop mix-blend-multiply and either ship transparent-background PNG/SVG marks, or wrap the logos in a light plate (bg-white/90 rounded px-3 py-2) that holds in both themes. Add the strip to the dark-mode visual snapshot suite.

--- [P2] Calendar conflict detection is a headline proof chip with no register row
  WHERE: src/components/home/TrustStrip.tsx:11; src/components/home/Platform.tsx:192-201; absent from src/lib/claims.ts
  "Hard + soft conflict detection" is one of four chips in the home trust strip, and Platform.tsx carries a "Conflict-aware calendar" card with a rendered "Hard conflict, Schlegel 207, 5:00 to 6:30p" example. No claim in claims.ts covers conflict detection — not live, not ci-verified, not roadmap — so a capability advertised in the first viewport has no evidence reference, no owner and no review date, while far less prominent features (delegation SLAs, budget import) each have one. The register's stated contract is that "every material claim the public site makes... has a row here."
  FIX: Add a claim row with the implementation and test references for hard/soft conflict detection, or pull the chip. Extend claims.spec's metric rule to cover trust-strip chips so a chip cannot ship without a claimId.

--- [P3] "Contact Sales" survives in two places after the site standardised on "Book a walkthrough"
  WHERE: src/components/site/SiteFooter.tsx:44-52; src/components/home/Faq.tsx:92-96
  site.ts:23-27 documents the decision: "One conversion phrase, used everywhere. 'Contact Sales' oversold a two-founder company and set the wrong expectation for who picks up." The footer's Get started link still reads "Contact Sales" and the FAQ closer still reads "Contact sales." Both route to /contact, whose own header says you will be talking to a founder — so the site sets the expectation it deliberately decided to stop setting, in the two places a skimming reader is most likely to click.
  FIX: Use site.ctaLabel in both (ContactSalesLink already defaults to it — just drop the children). Add a nav.spec assertion that the string "Contact Sales" appears nowhere in rendered text.

--- [P3] The claims register — the site's credibility artifact — carries a stale location row and a dead component
  WHERE: src/lib/claims.ts:599; src/components/site/SupporterStrip.tsx (unreferenced)
  C-022 records that the Simon and Startup Wednesday marks appear in "components/site/SupporterStrip.tsx", "/" and "/story". SupporterStrip is imported by nothing (grep returns only its own definition and the register row), the marks actually render in TrustStrip, and /story shows them as text rows, not images. It is a small thing, but this register is the document the founders would hand to a diligence reader as proof of rigour, and its first checkable coordinate is wrong.
  FIX: Delete SupporterStrip.tsx, update C-022's `where` to ["components/home/TrustStrip.tsx", "/"], and have claims.spec assert that every component path listed in a `where` field exists and is imported somewhere.

========================================================================================
## Impatient visitor on a mid-range Android phone (≈360–412 CSS px wide, DPR 2–3, Adreno/Mali-class GPU) on a congested campus network. I want to know what this is and hit the CTA within about eight seconds. I judged the site by reading the source and reasoning about what the browser actually has to do.
========================================================================================
FIRST TRUST FAILURE: The gap between the comments and the composition. ContourField.tsx:27-40 argues in detail that "every emitted character ships twice" and that the grid was cut to "about a third as much" — and then the home page mounts eight instances totalling 95,729 characters of path data, two of them byte-identical duplicates of seeds already on the page. Same pattern in SmoothScroll.tsx, whose comment says Lenis is "enabled only for real users" while on a phone it changes nothing (syncTouch defaults false) and costs a full remount of the document. The files argue the optimization carefully; the assembly undoes it. That made me stop trusting the other performance claims in the comments.
FIRST COMPREHENSION FAILURE: The evidence band. The prerendered HTML reads "0 organizations modelled / 0-gate approval chain, 7 request types / 0 records deleted at offboarding / 0 end-to-end tests" immediately under "Every number here is counted, not projected." NumberFlow renders its server value for real (NumberFlow-client-BTpPLmzo.mjs:101), and MetricsBand.tsx:21 hands it 0 because `useInView` is false during SSR. Until the bundle boots and I scroll 40% of that section into view, the page's proof section is four zeros.
FIRST BLOCKED ACTION: Stopping the hero dashboard's 4.2-second auto-tour. On a phone the module nav that would pause it is `hidden ... sm:flex` (DashboardMock.tsx:401) and the only thing that sets `paused` is `onMouseEnter` (DashboardMock.tsx:366), which a touchscreen never fires. The card cycles through five modules forever with no control, and it keeps cycling after I have scrolled ten sections past it.
WOULD NOT SURVIVE A DEMO: Two things. First, view-source on the live home page during a demo shows four rendered zeros under "Every number here is counted, not projected" — an easy question to get asked and an awkward one to answer on a site whose entire pitch is a governed claims register. Second, in dark mode the two supporter logos in TrustStrip should be effectively invisible: `mix-blend-multiply` (TrustStrip.tsx:33) over `bg-cloud`, which resolves to `--surface: oklch(21.5% 0.021 250)` in dark, multiplies a navy University of Rochester lockup into the background. The one piece of institutional proof on the page disappears for any visitor whose phone is on dark theme, which on a campus at night is most of them.

--- [P0] SmoothScroll flips the React element type after hydration, tearing down and rebuilding the entire document
  WHERE: C:\Users\adiab\tenure-landing\src\components\site\SmoothScroll.tsx:23
  SmoothScroll renders `<>{children}</>` on the first client render and `<ReactLenis root>{children}</ReactLenis>` one requestAnimationFrame later. Those are different element types at the same tree position, so React does not preserve anything: it unmounts SiteHeader + <main> + SiteFooter (the whole ~453KB DOM) and mounts a fresh copy. ReactLenis with `root` renders no DOM wrapper (node_modules/lenis/dist/lenis-react.mjs:110 returns the children directly through a context provider), so the wrapper is not even the point — only the type change is, and it is enough. Consequence on a mid-range Android: the browser builds, styles, lays out and paints ~6,000 SVG path nodes twice; every effect on the page runs twice (57 IntersectionObservers created/disconnected/recreated, DashboardMock's 4200ms interval cleared and restarted, SiteHeader's scroll listener re-added, motion's whileInView re-registered). It lands exactly at the moment the user first tries to scroll or tap, which is the worst possible moment for a several-hundred-millisecond main-thread block. It is invisible in CI because the `!navigator.webdriver` guard on line 18 means Playwright never takes this branch — no visual, interaction or a11y spec ever exercises the remount path.
  FIX: Never change the element type. Render `<ReactLenis root options={{ smoothWheel: smooth, lerp: 0.09 }}>{children}</ReactLenis>` unconditionally and let the `smooth` state flow into `options` instead of into the branch. Better still for this persona: mount Lenis from a zero-DOM sibling component (`new Lenis()` in an effect, `lenis.destroy()` on cleanup) so the page tree never has a Lenis node in it at all.

--- [P0] About 40% of the 453KB home document is decorative contour path text, and a quarter of that is a verbatim duplicate
  WHERE: C:\Users\adiab\tenure-landing\src\components\visuals\ContourField.tsx:139
  The home page mounts eight ContourField instances: Hero.tsx:35 (seed 1), Problem.tsx:64 (5), Platform.tsx:86 (2), Handoff.tsx:157 (5), AiOnboarding.tsx:52 (11), OfficeConsole.tsx:165 (11), MetricsBand.tsx:60 (4), CtaBand.tsx:26 (3). I re-ran buildContours for those exact seeds: 95,729 characters of `M…L…` path data across 5,982 separate line segments (11,964 path commands), before any surrounding markup. Because these are server components the same strings are emitted again in the RSC flight payload, so the true cost is ~187KB of the stated 453KB — for backgrounds that render between 5% and 13% opacity. Worse, seed 5 and seed 11 each appear twice, so 23,264 characters (≈45KB with the RSC copy) is byte-identical data shipped a second time for no reason. The ContourField header comment carefully argues the 66x38 → 48x28 reduction and then the page composition quietly multiplies the result by eight. Over the wire this brotli-compresses well (repetitive integer coordinates), but the parse and rasterize cost does not compress: the browser still has to build 5,982 sub-paths, and each carries `strokeLinecap="round"` (ContourField.tsx:147), so every one of those short segments gets two round caps rasterized — invisible at 1px width and 6% opacity, and the most expensive cap mode.
  FIX: Move generation out of the request entirely: a build script writes six unique fields (seeds 1,2,3,4,5,11) to public/contour-{seed}.svg, and SectionContour becomes a div with `background-image: url(/contour-5.svg)` plus the existing mask. That is zero bytes of HTML, zero bytes of RSC payload, brotli-compressed and immutably cached, shared across every page and every duplicate instance, and the browser rasterizes one image instead of building 6,000 DOM nodes. If you want to keep server generation, the minimum fix is one `<svg><defs>` per unique seed with `<use href="#c5">` at each call site, plus `strokeLinecap="butt"`.

--- [P1] Lenis ships, hydrates and runs a permanent rAF loop on phones, where it changes nothing about scrolling
  WHERE: C:\Users\adiab\tenure-landing\src\components\site\SmoothScroll.tsx:16
  Lenis defaults to `syncTouch: false` (node_modules/lenis/dist/lenis.mjs:433), and the event gate at lenis.mjs:615 is `if (!(syncTouch && isTouch || smoothWheel && isWheel)) return;`. A touchscreen produces no wheel events, so on the target device Lenis intercepts nothing and scrolling is exactly native. What the visitor pays for that: ~32KB of JS, the ReactLenis mount, the full-document remount above — and a rAF loop that never idles, because ReactLenis defaults `autoRaf = true` (lenis-react.mjs:45) and `Lenis.raf` unconditionally re-schedules itself (lenis.mjs:724) whether or not anything is animating. That is a 60Hz main-thread wake-up for the entire session, on a device whose scheduler badly wants to idle, delivering zero user-visible behaviour.
  FIX: Add a pointer check to the gate: `if (!navigator.webdriver && !reduced && window.matchMedia('(pointer: fine)').matches) setSmooth(true)`. Coarse-pointer devices then never construct Lenis at all and the module can be dynamically imported so it stays out of the initial chunk.

--- [P1] MetricsBand server-renders 0 for all four metrics, directly under "Every number here is counted, not projected"
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\MetricsBand.tsx:21
  Tile computes `const value = inView || reduce ? metric.value : 0` (line 21) and hands it to NumberFlow (line 29). `useInView` returns false during SSR and `useReducedMotion` returns null, so on the server `value` is 0 for every tile. @number-flow/react is not inert on the server — NumberFlow-client-BTpPLmzo.mjs:101 does `dangerouslySetInnerHTML: BROWSER ? '' : renderInnerHTML(data)`, so the prerendered HTML literally contains the rendered digit 0. The shipped home page therefore reads "0 organizations modelled", "0-gate approval chain, 7 request types", "0 records deleted at offboarding", "0 end-to-end tests" under a heading that says every number is counted. That is what view-source shows, what a reader-mode parser shows, what a crawler's first fetch shows, and what a phone shows if the bundle stalls. It is the same failure the team just fixed for Reveal (see the header comment at Reveal.tsx:10-20 about 118 elements at opacity:0) reintroduced through a different component. Secondary fragility: `amount: 0.4` on a section that is roughly 1,200–1,400 CSS px tall on a 360px-wide phone leaves only ~80px of headroom against a ~640px viewport.
  FIX: Render the true value on the server and animate from it: `const [seen, setSeen] = useState(false)` starting at `metric.value`, or simply pass `value={metric.value}` always and use NumberFlow's own `willChange`/trend to animate on mount. If you want the count-up, gate it with `useState(false)` flipped in an effect so SSR emits the real number and only the client ever sees 0. Also drop `amount: 0.4` to `amount: 0.15` or use a plain rootMargin.

--- [P1] The hero's supporting copy and BOTH call-to-action buttons are opacity:0 until the Reveal client component hydrates
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\Hero.tsx:66
  globals.css:468 sets `.js [data-reveal] { opacity: 0; transform: translateY(...) }`, and `.js` is added by the blocking inline head script (ThemeScript.tsx:25) before first paint. Hero.tsx wraps the subhead in Reveal at line 56, the CTA row (ContactSales + "Explore the platform") at line 66, and the pilot line at line 75. So the first paint on a slow phone is: headline, and nothing else — no description, no button. Those elements only become visible when the `Reveal` client component hydrates and its callback ref attaches an IntersectionObserver (Reveal.tsx:42-63). The component's own header comment defends this design against the "JavaScript unavailable" case, which it does handle, but the far more common case on a congested campus network is JavaScript *slow*, and in that case the primary conversion control is invisible for as long as hydration takes — behind framer-motion, number-flow, lenis and the full-document remount above.
  FIX: Do not wrap anything in the first viewport in Reveal — the subhead and CTA row should paint with the h1, exactly as the headline now does. For the rest of the page, add a CSS-only safety net so nothing is ever permanently JS-gated: `.js [data-reveal] { animation: reveal-safety 1ms linear 2.5s forwards }` with the keyframe restoring opacity/transform, cancelled by `.is-revealed`.

--- [P1] backdrop-blur-xl on the fixed header is active for the entire scroll session and is the most expensive per-frame op on the page
  WHERE: C:\Users\adiab\tenure-landing\src\components\site\SiteHeader.tsx:92
  SiteHeader toggles to `bg-canvas/85 backdrop-blur-xl` as soon as `window.scrollY > 12` (line 30 sets the state, line 92 applies the class). Twelve pixels means it is on for essentially the whole visit. A full-width 24px backdrop-filter forces the compositor to read back the backdrop region and re-blur it on every frame the content beneath moves — and beneath it are 5,982 SVG paths, nine mask-image layers and two large blurred radial gradients. On an Adreno/Mali mid-tier GPU this is the classic Android scroll-jank signature. The two hero wash divs compound it: `blur-2xl` (40px) on a 46rem square and `blur-3xl` (64px) on a 26rem square (Hero.tsx:32-33) sit directly behind the LCP headline, so they must be rasterized before the first meaningful paint.
  FIX: Drop the blur on coarse pointers — `max-lg:bg-canvas/95 lg:backdrop-blur-xl` gives the same read with an opaque fill on phones. Replace the two hero blur divs with a pre-baked radial-gradient PNG/CSS gradient (a 40–64px blur on a soft radial gradient is visually a no-op; the gradient already has the falloff). While you are in this file, the scroll handler at line 30 runs `setScrolled(window.scrollY > 12)` on every scroll event with no rAF throttle — use a 1px sentinel div plus IntersectionObserver instead.

--- [P1] The hero dashboard auto-tours forever with no visibility gate and cannot be stopped on a touch device
  WHERE: C:\Users\adiab\tenure-landing\src\components\visuals\DashboardMock.tsx:355
  DashboardMock's interval fires every 4200ms (line 357-359) and is gated only on `auto`, `reduce` and `paused`. There is no IntersectionObserver and no `document.visibilityState` check, so it keeps swapping the whole module subtree through `AnimatePresence mode="wait"` (line 430) while the visitor is ten sections down reading the FAQ, and it keeps going when the tab is backgrounded. SeatMechanism has the same shape at a 3400ms cadence (line 111-115). On the phone the pause story is worse: `paused` is only ever set by `onMouseEnter` (line 366), which a touchscreen never fires, and the module nav that would also set it is `hidden ... sm:flex` (line 401), so below 640px there is no control at all. Auto-updating content that cycles indefinitely with no pause mechanism is a WCAG 2.2 SC 2.2.2 failure, and it is the first thing I wanted to stop and could not.
  FIX: Gate the interval on an IntersectionObserver over the mock's own container and on `document.visibilityState === 'visible'`, and add `onPointerDown={() => setPaused(true)}` alongside the mouse handlers plus a visible pause/play affordance (or a horizontal tab strip) at `<sm` so touch users have a control. Same two changes in SeatMechanism.tsx:111.

--- [P2] HeroShapes and HeroFloatingCards hydrate framer-motion and run infinite animations while display:none on every phone viewport
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\HeroShapes.tsx:129
  HeroShapes is `hidden lg:block` (line 129) and HeroFloatingCards is `hidden xl:block` (line 165). Neither is ever visible on a mid-range Android. Both are `"use client"`, both import motion/react, and both declare `repeat: Infinity` keyframe animations (HeroShapes.tsx:49, HeroFloatingCards.tsx:13) plus `will-change: transform` on elements that will never paint. So a phone downloads them, hydrates them, constructs their motion values, and schedules perpetual animations for four coloured squares and two cards it cannot see. HeroFloatingCards also carries `backdrop-blur` on two cards. This is pure decoration on the critical hydration path for the wrong device class.
  FIX: Load them behind a width check rather than a CSS class: `const wide = useSyncExternalStore(subscribe, () => matchMedia('(min-width: 1024px)').matches, () => false)` and `next/dynamic(..., { ssr: false })` so the module never enters the phone's bundle. Cheaper interim fix: keep the CSS class but wrap the motion props so `repeat: Infinity` is only set when the media query matches.

--- [P2] The two supporter logos are fetched at 3840px wide to be displayed at about 128px
  WHERE: C:\Users\adiab\tenure-landing\src\components\home\TrustStrip.tsx:27
  TrustStrip passes the intrinsic dimensions straight through: `width={s.width}` with `site.supporters` declaring 2000x563 for Startup Wednesday and 1054x339 for Simon (site.ts:90-108), while displayHeight is 2.25rem / 2.15rem — about 128 and 112 CSS px wide. There is no `sizes` prop, so next/image builds a fixed srcset of `[width, width*2]` snapped to deviceSizes: 2048w 1x / 3840w 2x for Startup Wednesday and 1080w 1x / 3840w 2x for Simon. Every DPR-2-or-higher phone — i.e. all of them — picks the 3840px candidate for both. On a congested campus network that is two large re-encodes for two small marks; and if the deploy target is not running the Next optimizer, you ship the raw 75KB and 49KB PNGs at full resolution instead. Separately, `mix-blend-multiply` on line 33 over `bg-cloud` (= --surface, oklch(21.5%) in dark) multiplies a navy lockup into a near-black surface — in dark mode both supporter logos, the site's only social proof, go invisible.
  FIX: Give next/image the display size, not the intrinsic size: `width={128} height={36}` (and `width={112} height={36}`), or keep the intrinsic values and add `sizes="128px"`. Either produces a 128w/256w variant. Replace `mix-blend-multiply` with a theme-conditional treatment — `dark:invert dark:brightness-0 dark:opacity-80`, or ship a light-on-dark variant of each mark and swap on `[data-theme=dark]`.

--- [P2] 57 separate IntersectionObservers are constructed on the home page for identical reveal behaviour
  WHERE: C:\Users\adiab\tenure-landing\src\components\ui\Reveal.tsx:51
  Every `<Reveal>` is its own client component instance and constructs its own IntersectionObserver in its ref callback (Reveal.tsx:51) with options that are byte-identical everywhere: `{ rootMargin: '0px 0px -72px 0px' }`. Counting the home tree — Hero 3, TrustStrip 1, Problem 5, SeatMechanism 5, Platform 14, Handoff 6, AiOnboarding 6, OfficeConsole 7, Integrations 4, MetricsBand 1, Faq 2, CtaBand 3 — that is 57 observers created at hydration, and then created a second time after the SmoothScroll remount. Each observer runs its own intersection computation pass against the same root. 117 across the site.
  FIX: Hoist one module-level observer and a WeakMap<Element, () => void>: `const io = new IntersectionObserver(entries => {...}, OPTS)` created lazily on first use, with each Reveal calling `io.observe(el)` and `io.unobserve(el)` on cleanup. One observer, 57 observed targets, one intersection pass.

--- [P2] Ribbons applies an SVG feGaussianBlur to a 4-stroke group with a 140% filter region
  WHERE: C:\Users\adiab\tenure-landing\src\components\visuals\Ribbons.tsx:216
  Faq mounts Ribbons inside a `mask-image: linear-gradient` container sized `h-[82%] w-[60%]` (Faq.tsx:53-58). Ribbons wraps all four strokes — one of them 64px wide — in `filter="url(#rb-soft)"` with `feGaussianBlur stdDeviation="7"` and a `-20% / 140%` filter region (Ribbons.tsx:216-221). SVG filters are software-rasterized on a large share of Android Chrome installs and are re-run whenever the layer is invalidated. Stacked under a mask-image layer, in a section the user scrolls through, this is a second concrete jank source after the header blur. It is also the ninth mask-image layer on the page (eight SectionContours plus this one), each of which forces its own composited masked layer sized to a section.
  FIX: Bake it. This SVG is fully static — render it once at build time to public/ribbons.svg with the blur applied, or just export a WebP. Failing that, move the blur off the SVG filter and onto the wrapper as CSS `filter: blur(7px)`, which Chrome composites on the GPU, and drop the mask-image in favour of a matching alpha gradient inside the asset itself.

--- [P2] Four font files are preloaded at highest priority; three of them are for eyebrows and labels
  WHERE: C:\Users\adiab\tenure-landing\src\lib\fonts.ts:20
  fonts.ts self-hosts GeneralSans-Variable.woff2 (38KB) and pulls IBM Plex Mono at weights 400, 500 and 600. IBM Plex Mono on Google Fonts is not a variable font, so that is three separate static woff2 files, and next/font preloads by default — four `<link rel="preload" as="font" crossorigin>` at the top of the head, competing with the render-blocking stylesheet and the 453KB HTML for a congested connection's first round trips. General Sans is the only one on the LCP path; Plex Mono renders `label-mono` eyebrows, the `tnum` figures inside the dashboard mock, and small captions — all `display: swap`, none of it above-the-fold-critical.
  FIX: Add `preload: false` to the plexMono call. It keeps `display: swap`, so the mono text paints in the ui-monospace fallback and upgrades when the file arrives, and the critical path drops from four font requests to one. Consider also dropping weight 600 and reusing 500 for `font-semibold` mono, which removes a third of the remaining mono payload.

--- [P3] touchMultiplier: 1.4 is dead configuration
  WHERE: C:\Users\adiab\tenure-landing\src\components\site\SmoothScroll.tsx:26
  SmoothScroll passes `touchMultiplier: 1.4` into Lenis options. That value is only ever read inside the touch branch at lenis.mjs:328-329, which is reachable only when `syncTouch` is true (guards at lenis.mjs:597 and 626). syncTouch is left at its default of false. So the one option in this file that reads as if it were tuned for phones has no effect on phones — which is probably why nobody noticed that Lenis does nothing there at all.
  FIX: Delete `touchMultiplier: 1.4`, or set `syncTouch: true` if momentum on touch was actually the intent — but do not do the latter without measuring, because syncTouch moves scrolling onto the main thread and on this DOM it will be worse than native.

--- [P3] Every button carries a `sheen` class that is defined nowhere
  WHERE: C:\Users\adiab\tenure-landing\src\components\ui\Button.tsx:9
  The shared button base string starts with `"sheen group/btn relative ..."`. There is no `@utility sheen` in globals.css and no other definition anywhere in src — I grepped the whole tree and Button.tsx:9 is the only hit. So every Button, every ContactSales CTA and every Scheduler control ships a class name that resolves to nothing. Harmless at runtime, but it is residue from the refinement pass sitting on the site's most-used component, and the next person to touch button styling will spend time looking for it.
  FIX: Either remove `sheen ` from the base string, or restore the utility it refers to. If the hover sheen is wanted back, define it as an `@utility` guarded by `@media (hover: hover)` so it never costs a pseudo-element paint on touch.

--- [P3] 23MB of unused dependency and 1.3MB of unreferenced images
  WHERE: C:\Users\adiab\tenure-landing\package.json:16
  `simple-icons` is in package.json dependencies (23MB installed) and is imported by nothing in src — the tool marks in Integrations.tsx and ToolLogos.tsx are hand-authored files under public/logos/tools/ instead. public/photos/grass-soft.jpg (784KB) and public/photos/green-leaves.jpg (518KB) are referenced from no component and no CSS rule. None of this reaches the visitor, so it costs install time and CDN storage rather than load time, but it is the sort of thing that quietly turns into a real regression the first time someone writes `import { siExcel } from 'simple-icons'` in a client component.
  FIX: `npm rm simple-icons` and delete the two unreferenced photos. If simple-icons is wanted later, import from the per-icon path (`simple-icons/icons/microsoftexcel`) in a server component only.